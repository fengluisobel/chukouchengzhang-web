{
  "name": "chukouchengzhang-app",
  "version": "0.1.0",
  "private": true,
  "description": "出口成章 - 可运行产品原型（Node + 原生 Web）",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test tests/api.test.js",
    "smoke:flow": "bash scripts/smoke-api-flow.sh",
    "verify": "npm test && bash scripts/smoke-api-flow.sh"
  }
}

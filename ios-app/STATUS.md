# 出口成章 iOS App｜状态看板

最后更新时间：2026-04-21 20:20 Asia/Shanghai

## 当前阶段
已从“SwiftUI Demo 骨架”推进到“可在 Mac 上直接接手的 Xcode 工程骨架 + 后端联调链路已验证”的阶段。

## 已完成
- 本轮严格只做了 1 个小验证：直接解包最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz` 并检查包内 `ios-app/Mac-编译失败排障.md`；结果已真实命中增强版 verify 文案——不仅写到 `npm test + smoke-flow`，还明确“除 `transcribe / create / train / archive / report` 写链路外，会回读 `transcripts / training / ideas` 列表与 `bootstrap / reports-daily` 契约，并顺手校验 iOS 依赖字段”。最小证据：`tar -xOf dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz chukouchengzhang-mac-handoff-20260420-100846/ios-app/Mac-编译失败排障.md | rg -n 'transcripts / training / ideas|bootstrap / reports-daily|npm test \+ smoke-flow'` 命中 15 / 109 行。说明这份最新完整交付物里给 Mac 接手人看的排障主文档也已经跟上源码，不再只停留在 README / 首编手册层对齐；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小验证：直接解包最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz` 并检查包内 `ios-app/Mac-首编手册.md`；结果已真实命中增强版 verify 文案——不仅写到 `scripts/handoff-backend.sh verify` / `--backend-check verify`，还明确“除 `transcribe / create / train / archive / report` 写链路外，会回读 `transcripts / training / ideas` 列表与 `bootstrap / reports-daily` 契约，并顺手校验 iOS 依赖字段”。最小证据：`tar -xOf dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz chukouchengzhang-mac-handoff-20260420-100846/ios-app/Mac-首编手册.md | rg -n 'transcripts / training / ideas|bootstrap / reports-daily|--backend-check verify'` 命中 57 / 80 行。说明这次不只是源码侧文档对齐，连当前最新完整交付物里给 Mac 接手人看的首编主手册也已经跟上；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `README.md` 里脚本索引对 `scripts/mac-first-run.sh` 的那条说明补齐到和当前 `scripts/mac-first-run.sh --help` / README 主文一致，明确默认 `verify` 不只是“同级 `app/` 存在时跑 `npm test + smoke-flow`”，还会回读 `transcripts/training/ideas` 与 `bootstrap/reports-daily`，并顺手校验 iOS 依赖字段。随后只做了最小验证：`rg -n 'scripts/mac-first-run\.sh|transcripts/training/ideas|bootstrap/reports-daily|iOS 依赖字段' README.md` 命中新增索引文案，且 `bash scripts/mac-first-run.sh --help | rg -n 'transcripts/training/ideas|bootstrap/reports-daily|iOS 依赖字段'` 也命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小验证：直接核对 `ChuKouChengZhang/Services/APIClient.swift` 里 iOS 客户端真实会打到的后端接口，与 `app/src/server.js` 当前实际暴露的路由是否一一对应。最小证据：用一条 Python 对照脚本抽出客户端 10 条真实调用（`GET /api/bootstrap`、`/api/ideas`、`/api/provider/status`、`/api/reports/daily`、`/api/training`、`/api/transcripts`，以及 `POST /api/ideas/archive`、`/api/train/evaluate`、`/api/transcribe`、`/api/transcripts/create`），结果 `SERVER_HAS_ALL_CLIENT_ROUTES = True`、`MISSING = []`。这说明至少在“iOS 客户端当前实际调用的路径/方法”这一层，后端没有再发生路由漂移；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `Mac-环境安装清单.md` 里“当前最新完整 Mac 交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处入口，从已过时的 `chukouchengzhang-mac-handoff-20260420-034637.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-mac-handoff-20260420-100846.tar.gz`，并连带把对应 `.sha256` 路径一起更新。这个点很小，但它正好落在 Mac 接手第一落点文档上；如果这里继续挂上一版完整包名，接手人会优先拿到已经落后于最新状态看板的旧交付物。随后只做了最小验证：`rg -n '20260420-034637|20260420-100846' Mac-环境安装清单.md` 现仅剩 `20260420-100846`，且 `ls dist | rg 'chukouchengzhang-mac-handoff-20260420-100846\\.tar\\.gz(\\.sha256)?$'` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小验证：重新对照 `Mac-首编手册.md` 与 `bash scripts/mac-first-run.sh --help`，确认首编主文档里关于 `scripts/handoff-backend.sh verify` / `--backend-check verify` 的两处说明，当前已经和脚本真实帮助保持一致——都明确写到这条总自检除 `transcribe / create / train / archive / report` 写链路外，还会回读 `transcripts / training / ideas` 列表与 `bootstrap / reports-daily` 契约，并顺手校验 iOS 依赖字段。最小证据：`rg -n 'transcripts / training / ideas|bootstrap / reports-daily|--backend-check verify' Mac-首编手册.md` 命中 57 / 80 行，而 `bash scripts/mac-first-run.sh --help | rg -n 'transcripts/training/ideas|bootstrap/reports-daily|--backend-check verify'` 也同步命中 4 / 10 / 21 行。说明首编主路径文档与实际脚本帮助在这个点上没有再漂。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小验证：按上轮已锁定的断点，重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新的完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz`（以及同名 `.zip` 与对应 `.sha256`）；打包流程再次真实走通了 `npm run verify`、静态工程校验与归档命中检查。随后只补这一条最小证据：直接解包检查新包内 `HANDOFF-README.md`，现已真实命中增强版 verify 文案——不仅写明 `verify = npm test + smoke-flow`，还明确“除 `transcribe/create/train/archive/report` 写链路外，会回读 `transcripts/training/ideas` 列表与 `bootstrap/reports-daily` 契约，并顺手校验 iOS 依赖字段”。这说明上轮确认的“最新完整包 README 说明层落后于源码”这个交付断点，这轮已经在最终产物上闭环。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小验证：继续钉住“为什么最新完整包里的 `HANDOFF-README.md` 还是旧文案”这个断点，补了一条更硬的时间证据。结果不是脚本没改，而是**当前完整包本来就早于这次模板更新**：`stat` 显示 `dist/chukouchengzhang-mac-handoff-20260420-034637.tar.gz` 打包时间是 `2026-04-20 03:46:46 +0800`，而 `scripts/package-mac-handoff.sh` 当前模板文件修改时间是 `2026-04-20 08:06:05 +0800`。同时再次直接解包该 tar.gz，包内 `HANDOFF-README.md` 仍停留在旧表述“verify = npm test + smoke-flow，会覆盖 transcribe/create/train/archive/report 全链路，并顺手校验 iOS 依赖字段”，而源码模板已改成“除写链路外，还会回读 `transcripts/training/ideas` 与 `bootstrap/reports-daily`”。结论更清楚了：这不是状态看板误判，而是**最新完整产物尚未重打**；下一轮最小推进仍应是只做一件事——重新执行 `bash scripts/package-mac-handoff.sh`，把最新 `HANDOFF-README.md` 模板真正带进完整 Mac 交接包。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小验证：直接解包最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260420-034637.tar.gz` 并检查包内 `HANDOFF-README.md`；结果源码 `scripts/package-mac-handoff.sh` 模板里的增强版 verify 说明（会回读 `transcripts/training/ideas` 与 `bootstrap/reports-daily`）**还没有**真实进入当前产物。最小证据：源码 `rg -n 'transcripts/training/ideas|bootstrap/reports-daily|verify = npm test \+ smoke-flow' scripts/package-mac-handoff.sh` 命中 202 / 207 行，但包内 `HANDOFF-README.md` 只命中旧表述“verify = npm test + smoke-flow，会覆盖 transcribe/create/train/archive/report 全链路，并顺手校验 iOS 依赖字段”。说明最新完整 handoff 包在 `HANDOFF-README.md` 这一层仍落后于源码；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。不过眼前最近的交付缺口已经收缩成：**需要再重打一版完整 Mac handoff 包，把最新 `HANDOFF-README.md` 文案真正带进产物。**
- 本轮严格只做了 1 个小改动：把 `scripts/package-mac-handoff.sh` 里生成 `HANDOFF-README.md` 的两处后端 `verify` 说明补齐到和当前 `scripts/mac-first-run.sh --help`、`scripts/handoff-backend.sh --help`、根 `README.md` 真实行为一致，明确这条总自检不只覆盖 `transcribe/create/train/archive/report` 写链路，还会回读 `transcripts/training/ideas` 列表与 `bootstrap/reports-daily` 契约，并顺手校验 iOS 依赖字段。随后只做了最小验证：`bash -n scripts/package-mac-handoff.sh` 通过，且 `rg -n 'transcripts/training/ideas|bootstrap/reports-daily|verify = npm test \+ smoke-flow' scripts/package-mac-handoff.sh` 命中 202 / 207 行。这个改动还没有重打完整 Mac handoff 包，所以新文案会在**下一次**执行 `bash scripts/package-mac-handoff.sh` 后进入最终交付物；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `README.md` 里 `scripts/mac-first-run.sh` 那段关于默认 `verify` / `--backend-check verify` 的两句说明补齐到和当前 `scripts/mac-first-run.sh --help`、`scripts/handoff-backend.sh --help` 真实行为一致，明确它不只覆盖 `transcribe/create/train/archive/report` 写链路，还会回读 `transcripts/training/ideas` 列表，并顺手校验 `bootstrap` / `reports-daily` 与 iOS 依赖字段。这个点很小，但它正好落在很多接手人第一眼会看的根 README；如果这里只写“全链路 + iOS 字段”，仍会低估 verify 对历史列表、训练记录、灵感库与首页聚合回读契约的覆盖范围。随后只做了最小验证：`rg -n 'transcripts/training/ideas|bootstrap|reports-daily' README.md` 命中新增文案，其中根 README 的默认 verify / `--backend-check verify` 说明已同步落在 104 / 105 行。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把 README 主路径的自解释能力又补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `Mac-编译失败排障.md` 里两处后端 `verify` 的说明补齐到和当前 `scripts/handoff-backend.sh --help`、`scripts/mac-first-run.sh` 真实行为一致，明确这条总自检不只覆盖 `transcribe / create / train / archive / report` 写链路，还会回读 `transcripts / training / ideas` 列表与 `bootstrap / reports-daily` 契约，并顺手校验 iOS 依赖字段。随后只做了最小验证：`rg -n 'transcripts / training / ideas|bootstrap / reports-daily' Mac-编译失败排障.md` 命中 2 处，且 `bash scripts/handoff-backend.sh --help | rg 'transcripts/training/ideas|bootstrap / reports-daily'` 也命中。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把“Mac 上一旦首编或联调卡住时，排障主文档对 verify 覆盖范围的解释”补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `Mac-环境安装清单.md` 里三处 `verify` 说明补齐到和当前 `scripts/mac-first-run.sh --help`、`scripts/handoff-backend.sh --help` 的真实行为一致，明确它不只覆盖 `transcribe / create / train / archive / report` 写链路，还会回读 `transcripts / training / ideas` 列表与 `bootstrap / reports-daily` 契约，并顺手校验 iOS 依赖字段。这个点很小，但它正好落在 Mac 接手第一落点文档上；如果这里只写成“`npm test + smoke-flow` + iOS 依赖字段”，接手人仍会低估默认 verify 对首页聚合、历史列表、训练记录与灵感库回读契约的覆盖范围。随后只做了最小验证：`bash scripts/mac-first-run.sh --help` 与 `bash scripts/handoff-backend.sh --help` 已命中增强版 verify 文案，且 `rg -n 'transcripts / training / ideas|bootstrap / reports-daily' Mac-环境安装清单.md` 也命中 3 处。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把“第一落点文档对 verify 覆盖范围的自解释能力”补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `Mac-环境安装清单.md` 里“当前最新完整 Mac 交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处文件名，从过期的 `chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-mac-handoff-20260420-034637.tar.gz`，并保留对应 `.sha256` 路径一致。这个点很小，但它正好落在 Mac 接手第一落点文档上；如果这里继续挂旧包名，接手人会优先拿到落后于当前状态看板的旧交付物。随后只做了最小验证：`rg -n "20260405-001100|20260420-034637" Mac-环境安装清单.md` 仅剩新包名，且 `ls dist | rg 'chukouchengzhang-mac-handoff-20260420-034637\\.tar\\.gz(\\.sha256)?$'` 命中。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把“下载入口指向最新完整交付物”这件事补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `scripts/mac-first-run.sh --help` 里默认 `verify` / `--backend-check verify` 的说明补齐到和当前真实行为一致，明确它不只覆盖 `transcribe/create/train/archive/report` 写链路，还会顺手回读 `transcripts/training/ideas` 列表与 `bootstrap/reports-daily` 契约。这样 Mac 接手人如果第一步只跑 `bash scripts/mac-first-run.sh --help`，不会再把这条默认后端总自检误解成“只测写链路”的浅检查。随后只做了最小验证：`bash scripts/mac-first-run.sh --help | grep -E 'transcripts/training/ideas|bootstrap/reports-daily|--backend-check verify'` 命中。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把最常见首编入口的自解释能力又补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `Mac-环境安装清单.md` 里“一键首编预热”这段也补上了 `scripts/mac-first-run.sh --strict-env-check` 的主路径用法；现在只看环境安装清单的 Mac 接手人，如果想把 `mac-env-check.sh` 里的 WARN 一并当成阻断项，不必再自己从 `Mac-首编手册.md` 或脚本 `--help` 里反查，可以直接照抄 `bash scripts/mac-first-run.sh --strict-env-check --skip-signing` 这条更严格的首编预热命令。随后只做了最小验证：`grep -n 'strict-env-check\|mac-env-check.sh --strict' Mac-环境安装清单.md` 命中，且 `bash scripts/mac-first-run.sh --help | grep -- '--strict-env-check'` 也命中。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把“严格预检”入口补进了 Mac 接手第一落点文档。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里 `scripts/mac-first-run.sh` 的主路径说明补上了 `--strict-env-check` 用法；现在接手人如果想走“一把串完环境预检 → 静态校验 → 后端检查 → 签名 → xcodebuild 烟测”的主路径，同时又想把 `mac-env-check.sh` 里的 WARN 也当成阻断项，不必自己从脚本帮助里二次翻参数，直接看首编主手册就能照抄 `bash scripts/mac-first-run.sh --strict-env-check ...`。随后只做了最小验证：`grep -n "strict-env-check\|mac-env-check.sh --strict" Mac-首编手册.md` 命中。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把一个已经存在于脚本、尚未进入首编主文档的关键入口补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `scripts/mac-first-run.sh` 补了 `--strict-env-check` 参数；现在接手人如果想走“一把串完环境预检 → 静态校验 → 后端检查 → 签名 → xcodebuild 烟测”的主路径，同时又想把 `mac-env-check.sh` 里的 WARN 也当成阻断项，不必再先手动单独跑一遍 `bash scripts/mac-env-check.sh --strict`，可以直接执行 `bash scripts/mac-first-run.sh --strict-env-check ...`。这次只做了最小验证：`bash -n scripts/mac-first-run.sh` 通过，且 `bash scripts/mac-first-run.sh --help` 已命中 `--strict-env-check` 说明。这个改动不解决真实签名、真机网络或 Xcode 首编本身，但把“严格预检也要走一把串联主路径”这层接手体验补齐了一格。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接检查当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 里的 `ios-app/scripts/mac-env-check.sh`，确认包内**没有**带上最近源码新增的 `xcrun --sdk iphonesimulator --show-sdk-path` / `iPhone Simulator SDK 可用` / `当前拿不到 iPhone Simulator SDK` 这层首编预检；随后再对照源码 `scripts/mac-env-check.sh`，已确认这组检查现在只存在于源码、不在最新完整 handoff 包里。说明当前最新完整交付物仍落后于源码：接手人如果直接拿这份包去 Mac，暂时还吃不到这层更早暴露 `xcode-select` / Simulator Runtime / 完整 Xcode SDK 问题的 fail-fast 保护。本轮没有新增代码推进，新增的是一个更明确的交付结论：**下一轮应优先重打一次完整 Mac handoff 包，把最新 `mac-env-check.sh` 真正带进产物。** 当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `scripts/mac-env-check.sh` 再补了一层更贴近真实 Xcode 首编的硬检查——现在不只确认 `xcrun` / `simctl` 在不在，还会先显式执行 `xcrun --sdk iphonesimulator --show-sdk-path`，要求当前 Mac 真能拿到 `iPhone Simulator SDK` 路径；如果完整 Xcode 还没装全、Simulator Runtime 还没落好，或 `xcode-select` 仍指错目录，就会在点开 Xcode / 跑 `xcodebuild` 之前直接 fail-fast，而不是拖到真正首编时才炸。随后只做了最小验证：`bash -n scripts/mac-env-check.sh` 通过。这个改动依旧没有跨过签名、真机网络或真实 Mac/Xcode 首编本身，但把一个非常靠前的 Mac 环境阻塞点又往前拦了一层；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `app/scripts/smoke-api-flow.sh` 里对 `/api/bootstrap` 的 verify 契约检查再补强一格；现在隔离 `verify` 主路径在 `bootstrap(before)` 与 `bootstrap(after)` 两次回读时，不只看 `providerStatus.ok`，还会显式要求 `providerStatus.provider / checkedAt` 也必须存在。因为 iOS 端首页聚合里的 `BootstrapDTO.providerStatus` 实际复用 `ProviderStatusDTO` 解码，而此前更轻量的 `ios-app/scripts/smoke-backend.sh` 已经兜住这 3 个字段，主验证链路却还没把这层自动拦住，仍存在“轻量 smoke 单独过了、verify 也过了，但 App 真到首页解码 bootstrap 时才暴露 providerStatus 少字段”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `scripts/mac-env-check.sh` 补了一条更贴近真实 Mac 首编坑的环境预检——如果 `xcode-select -p` 指向的是 `/Library/Developer/CommandLineTools`，脚本现在会明确给出 `WARN`，提醒先切回完整 Xcode 的 Developer 目录（`sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`），避免“命令行工具装了、但 iOS Simulator / xcodebuild 仍拿不到完整 Xcode SDK”这种情况在真正首编时才暴露。随后只做了最小验证：`bash -n scripts/mac-env-check.sh` 通过。这个改动不解决签名、真机网络或后端本身问题，但能把一个高频的 Mac 环境误配置更早拦在首编前。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接解包当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz`，并在包内 `ios-app/` 目录执行 `bash scripts/handoff-backend.sh --help | rg 'transcripts/training/ideas|bootstrap / reports-daily|transcribe/create/train/archive/report'`；结果只命中了旧说明“`transcribe/create/train/archive/report` 全链路”，**没有**命中最近源码里已补上的“还会回读 `transcripts / training / ideas` 列表，并顺手校验 `bootstrap / reports-daily` 契约”这层解释。说明当前最新完整 handoff 包虽然能跑 `verify`，但包内自带的 `handoff-backend.sh --help` 文案已经落后于源码；接手人如果只在 Mac 上看包内脚本帮助，仍会低估 verify 对首页聚合、历史列表、训练记录与灵感库回读契约的覆盖范围。本轮没有新增代码推进，新增的是一个更明确的交付结论：**下一轮应优先重打一次完整 Mac handoff 包，把最新帮助文案真正带进产物**。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `app/README.md` 里 `npm run verify` 的说明再补齐到当前真实行为；现在它不再只笼统写成“覆盖 `transcribe / create / train / archive / report` 全链路，并校验 iOS 关键字段”，而是明确补上：除了写链路，还会回读 `transcripts / training / ideas` 列表，并顺手复核 `bootstrap` / `reports/daily`。因为当前完整 Mac 交接包里，接手人不一定先翻 `ios-app/README.md` 或 `scripts/handoff-backend.sh --help`，也可能直接进 `app/README.md` 看后端总自检；如果这里仍只强调写链路，就会低估 verify 对首页聚合、历史列表、训练记录与灵感库回读契约的覆盖范围。随后只做了最小验证：`grep -nF '除 `transcribe / create / train / archive / report` 写链路外，还会回读 `transcripts / training / ideas` 列表，并顺手复核 `bootstrap` / `reports/daily` 与 iOS 客户端依赖的关键返回字段。' app/README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `app/scripts/smoke-api-flow.sh` 里 `reports/daily + bootstrap(after).report` 的契约检查再补强一格；现在 `npm run verify` / `bash scripts/handoff-backend.sh verify` 不只会盯 `transcribeCount / trainingCount / speechInputCount / bestSentence`，还会显式要求 `totalWords / polishCount / catchphraseCount` 这 3 个 `DailyReportDTO` 必需字段也必须在隔离 verify 主路径里存在。因为此前虽然最轻量 `smoke-backend.sh` 已经会查全量日报字段，但更关键的 `verify` 主路径还没把这层自动拦住，仍存在“轻量 smoke 单独过了，但 Mac 首编前最常跑的 verify 也过了，结果 App 真打开报告页或首页解码 `DailyReportDTO` / `bootstrap.report` 时才暴露 JSON 少字段”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `ios-app/README.md` 里 `bash scripts/handoff-backend.sh verify` 那句说明补齐到当前脚本真实行为；现在它不再只笼统写成“覆盖 `transcribe/create/train/archive/report` 全链路”，而是明确补上：除了写链路，还会回读 `transcripts / training / ideas` 列表，并顺手校验 `bootstrap` / `reports-daily` 与 iOS 依赖字段。这样 Mac 接手人如果只看 README 主路径，也不会低估这条后端总自检对首页、历史列表、训练记录与灵感库回读接口的覆盖范围。随后只做了最小验证：`grep -nF '除 `transcribe/create/train/archive/report` 写链路外，还会回读 `transcripts/training/ideas` 列表，并顺手校验 `bootstrap` / `reports-daily` 与 iOS 依赖字段' README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/handoff-backend.sh --help` 里 `smoke-flow / verify` 的说明补齐到当前真实后端接口覆盖范围；现在它不再只笼统写成 `transcribe/create/train/archive/report`，而是明确点出还会顺手校验 `health / provider-status / bootstrap / transcripts-list / training-list / ideas-list / reports-daily` 这组读链路与聚合契约。因为当前 Mac 接手人最可能直接先跑的就是 `bash scripts/handoff-backend.sh --help`，如果这里只强调“写链路”，仍会低估这条总自检对首页、历史列表、训练记录和灵感库回读接口的覆盖范围。随后只做了最小验证：`bash scripts/handoff-backend.sh --help` 输出已命中新说明。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `app/scripts/smoke-api-flow.sh` 里的 `/api/provider/status` 契约检查再补强一格；现在 `npm run verify` / `bash scripts/handoff-backend.sh verify` 不只会确认 `ok / checkedAt`，还会显式要求返回 `provider`，更贴近 iOS 端 `ProviderStatusDTO` 的真实解码需求。因为此前虽然轻量 smoke 已兜住 `provider`，但更重的隔离 verify 主路径还没把这层自动拦住，仍存在“轻量 smoke 过了、verify 也过了，但 App 真到设置页或首屏解码 `provider/status` 时才暴露 JSON 少字段”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `app/scripts/smoke-api-flow.sh` 末尾的 `bootstrap(after)` 聚合校验再补强一格；现在 `npm run verify` / `bash scripts/handoff-backend.sh verify` 在 `reports/daily` 之后，不只会确认首页聚合接口里 `transcripts / ideas` 条数回填正常，还会顺手核对 `providerStatus.ok`、`transcripts[0].id / summaryTitle / provider`、`ideas[0].id / title / nextAction`，以及 `report.transcribeCount / trainingCount / speechInputCount / bestSentence` 这组首页真实依赖字段。因为此前虽然已经分别校过 `/api/transcripts`、`/api/ideas`、`/api/reports/daily`，但还没把“写入后再回读 `/api/bootstrap` 聚合结果是否仍自洽”自动兜住，仍存在“分接口都正常、唯独首页聚合 JSON 悄悄漂了，要到 App 真打开首页才暴露”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `app/scripts/smoke-api-flow.sh` 补上 `GET /api/transcripts` 的契约检查；现在 `npm run verify` / `bash scripts/handoff-backend.sh verify` 在 `transcripts/create` 之后，会顺手确认历史列表返回 `id / createdAt / scene / mode / rawText / polishedText / issues` 这组 `TranscriptDTO` 必需字段，以及 `inputSource / summaryTitle / suggestedTags / nextAction / provider` 这组 iOS 首页与结果页依赖字段。因为此前虽然单独人工核过一次 `/api/transcripts`，但自动 verify 主路径还没把这层兜住，仍存在“创建成功、首屏 bootstrap 也正常，但历史列表单独回读 JSON 漂了，要到 App 真打开历史/首页列表时才暴露”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `app/scripts/smoke-api-flow.sh` 补上 `GET /api/ideas` 的契约检查；现在 `npm run verify` / `bash scripts/handoff-backend.sh verify` 在 `ideas/archive` 之后，还会顺手确认灵感库列表返回 `id / transcriptId / title / rawInput / normalizedText / category / tags / nextAction / status / createdAt` 这组 `IdeaDTO` 依赖字段。因为此前虽然单独人工核过一次 `/api/ideas`，但自动 verify 主路径还没把这层兜住，仍存在“归档能写入，但灵感库回读 JSON 漂了，要到 App 真打开灵感库页才暴露”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：给 `app/scripts/smoke-api-flow.sh` 补上 `GET /api/training?transcriptId=...` 的契约检查；现在 `npm run verify` / `bash scripts/handoff-backend.sh verify` 除了 `transcribe/create/train/archive/report` 主链路，还会顺手确认训练页回读列表返回 `id / transcriptId / round / text / clarityScore / structureScore / polishScore / feedback` 这组 `TrainingAttemptDTO` 依赖字段。因为此前虽然单独人工核过一次 `/api/training`，但自动 verify 主路径还没把这层兜住，仍存在“训练评分能写入，但训练页回读 JSON 漂了，要到 App 真打开训练历史才暴露”的缝。随后只做了最小验证：`bash -n app/scripts/smoke-api-flow.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里“一键首编脚本会先跑 `scripts/handoff-backend.sh verify`”那句说明补齐到和 `README.md`、`Mac-环境安装清单.md`、`scripts/mac-first-run.sh --help` 保持一致，明确它不是一句偏泛的“`npm test + smoke-flow`”，而是覆盖 `transcribe / create / train / archive / report` 全链路，并额外校验 iOS 依赖字段；因为我刚复查首编主手册时发现，这个入口虽已写明会跑 verify，但这一句仍比其余入口少半层，接手人如果只看主手册，还是可能低估默认首编前总自检的覆盖范围。随后只做了最小验证：`grep -n '覆盖 `transcribe / create / train / archive / report` 全链路，并额外校验 iOS 依赖字段' Mac-首编手册.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接检查当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/package.json`、`app/README.md`、`app/scripts/start.sh`、`app/scripts/status.sh`、`app/scripts/stop.sh`、`app/scripts/smoke-api-flow.sh` 这组“Mac 接手后端最常用入口”都已真实进包；因为这份最新完整交付物虽然此前已经验证过 `verify` 可跑、`HANDOFF-README.md` 文案也已更新，但这组最基础的后端操作入口还没对着 **当前最新包** 单独点名核过。随后只做了最小验证：`tar -tzf dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz | rg '^chukouchengzhang-mac-handoff-20260405-001100/app/(package\.json|README\.md|scripts/start\.sh|scripts/status\.sh|scripts/stop\.sh|scripts/smoke-api-flow\.sh)$'` 命中 6 条。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成 `HANDOFF-README.md` 时“`mac-first-run.sh` 会先跑后端 verify”那句说明，补齐到和 `README.md`、`Mac-首编手册.md`、`Mac-环境安装清单.md`、`scripts/mac-first-run.sh --help` 一致，明确它不只是覆盖 `transcribe/create/train/archive/report` 全链路，还会顺手校验 iOS 依赖字段；因为我刚反查当前最新完整包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 里的 `HANDOFF-README.md`，发现这句仍少半层，接手人如果只看包内说明，还是可能低估 verify 的覆盖范围。随后只做了最小验证：`bash -n scripts/package-mac-handoff.sh` 通过，且 `rg -n '后端 verify（`npm test \+ smoke-flow`，覆盖 transcribe/create/train/archive/report 全链路，并顺手校验 iOS 依赖字段）' scripts/package-mac-handoff.sh` 命中。注意：这次还**没有**重打完整 Mac handoff 包，所以这处新文案会在**下次**重新执行 `bash scripts/package-mac-handoff.sh` 后进入新归档。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 里两处后端 `verify` 的说明补齐到和 `README.md`、`Mac-首编手册.md`、`Mac-环境安装清单.md`、`scripts/handoff-backend.sh --help` 保持一致，明确它不只是 `npm test + smoke-flow` 的名字提示，而是覆盖 `transcribe/create/train/archive/report` 全链路，并顺手校验 iOS 依赖字段；因为接手人到了 Mac 上真卡住时，往往只翻排障文档，如果这里仍少一层，就还是可能低估这条总自检对接口契约回归的覆盖范围。随后只做了最小验证：`grep -n '顺手校验 iOS .*依赖字段' Mac-编译失败排障.md` 命中 2 处。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里 `--backend-check verify` 的说明补齐到和 README / `scripts/mac-first-run.sh --help` / `Mac-环境安装清单.md` 保持一致，明确它不只是“先跑 `npm test`，再跑 `smoke-flow`”，还会顺手校验 iOS 依赖字段；因为接手人如果只看首编主手册选后端检查强度，这里此前比其余入口少了一层，仍可能低估默认首编前后端总自检的覆盖范围。随后只做了最小验证：`grep -n "顺手校验 iOS 依赖字段" Mac-首编手册.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `README.md` 里 `scripts/mac-first-run.sh` 那段总述里的默认 `verify` 说明补齐到和同段 `--backend-check verify`、`Mac-首编手册.md`、`Mac-环境安装清单.md` 保持一致，明确它不只是 `npm test + smoke-flow` 的简称，而是覆盖 `transcribe/create/train/archive/report` 全链路，并顺手校验 iOS 依赖字段；因为接手人经常只看这段“一键首编会串什么”，如果这里仍写得比后面的参数说明更浅，就还是会低估默认首编前后端总自检的覆盖范围。随后只做了最小验证：`grep -nF '自动跑 verify（`npm test + smoke-flow`，覆盖 `transcribe/create/train/archive/report` 全链路，并顺手校验 iOS 依赖字段）' README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `README.md` 里“进入 `ios-app/` 后直接执行 `bash scripts/handoff-backend.sh verify`”那句说明补齐到和 `Mac-首编手册.md`、`Mac-环境安装清单.md`、`scripts/handoff-backend.sh --help` 保持一致，明确它不只是 `npm test + smoke-flow` 的短名，而是覆盖 `transcribe/create/train/archive/report` 全链路，并顺手校验 iOS 依赖字段；因为 README 这段正好处在“如何在 Mac 上接手”的主路径里，若这里比其余文档少一层，接手人仍可能低估直接跑 `verify` 的价值。随后只做了最小验证：`grep -nF '可直接从 `ios-app` 目录侧对同级 `app/` 后端做一轮总自检（`npm test + smoke-flow`，覆盖 `transcribe/create/train/archive/report` 全链路，并顺手校验 iOS 依赖字段）' README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成 `HANDOFF-README.md` 时“拆开执行后端”那段里的 `verify` 说明补齐到和 README / 首编手册 / 环境清单 / `handoff-backend.sh --help` 保持一致，明确它不只是覆盖 `transcribe/create/train/archive/report` 全链路，还会顺手校验 iOS 依赖字段；因为完整 Mac handoff 包里真正最容易被接手人第一眼照着执行的，往往就是这份自带 README，如果这里少这一层，仍可能让人低估首编前总自检的覆盖范围。当前还没重新打包，所以这处文案会在**下次**重新执行 `bash scripts/package-mac-handoff.sh` 后进入新的完整交接包；当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/handoff-backend.sh --help` 里 `verify` 的说明补齐到和 README / 首编手册 / 环境清单 / `app/README.md` 保持一致，明确它不只是“先跑 `npm test + smoke-flow`”，还会顺手校验 iOS 客户端依赖字段；因为不少接手人会先直接跑统一后端入口看 `--help`，如果这里比其余文档少一层，就仍可能低估 Mac 首编前总自检的覆盖范围。随后只做了最小验证：`bash scripts/handoff-backend.sh --help | grep -F -- 'verify：先跑 npm test，再跑包含 transcribe/create/train/archive/report 的 smoke-flow，并顺手校验 iOS 客户端依赖字段；适合 Mac 首编前做一轮后端接口契约总自检'` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/mac-first-run.sh --help` 里 `--backend-check verify` 的说明补齐到和 README / 首编手册 / 环境清单保持一致，明确它不只是“先跑 `npm test + smoke-flow`”，还会顺手校验 iOS 依赖字段；因为很多人拿到 Mac 后第一反应就是先跑脚本 `--help` 看参数，如果这里比文档少一层，会低估默认首编前总自检的覆盖范围。随后只做了最小验证：`bash scripts/mac-first-run.sh --help | grep -F -- '--backend-check verify         先跑 npm test，再跑 smoke-flow，并顺手校验 iOS 依赖字段'` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里三处仍偏简的 `verify` 说明补齐到和当前脚本真实行为一致，明确它不只是 `npm test + smoke-flow` 的名字提示，而是覆盖 `transcribe / create / train / archive / report` 全链路，并顺手校验 iOS 依赖字段；因为这份清单就是“拿到 Mac 先装环境、再首编”的第一落点，若这里不写透，接手人仍可能低估 `verify` 对接口契约回归的价值。随后只做了最小验证：`grep -nF 'iOS 依赖字段' Mac-环境安装清单.md` 命中 3 处。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `README.md` 里 `scripts/mac-first-run.sh --backend-check verify` 的说明补齐到和脚本真实行为一致，明确它不只是 `npm test + smoke-flow` 的名字提示，而是覆盖 `transcribe/create/train/archive/report` 全链路，并顺手校验 iOS 依赖字段；因为接手人很多时候先看的是根 README，而这里之前对 `smoke` 写得更细，对 `verify` 却还偏简，容易低估这条默认首编前后端总自检的覆盖范围。随后只做了最小验证：`grep -n "verify.*iOS 依赖字段" README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/mac-env-check.sh` 里“未检测到同级 app/”的结果从 `WARN` 改成不计入失败的 `NOTE`；也就是现在只带 `ios-app` 去 Mac 做“先编译 / Mock / 看 UI”时，即使跑 `bash scripts/mac-env-check.sh --strict`，也不会再因为**本来就没打算带后端**而被假阻断。这个点之所以值得补，是因为当前项目已经明确区分 iOS-only 路径与完整闭环路径，缺少同级 `app/` 对前者不是环境缺陷，只是能力边界；继续把它算成 WARN，会把“严格预检”误变成“必须先带后端”。随后只做了最小验证：`bash -n scripts/mac-env-check.sh` 通过，且新提示文案已落在脚本里。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成 `HANDOFF-README.md` 时“后端拆开执行”的建议顺序补成更贴近真实接手路径的写法，明确推荐先 `bash scripts/handoff-backend.sh verify`，再 `start` / `smoke`，并顺手写明 `verify = npm test + smoke-flow` 会覆盖 `transcribe/create/train/archive/report` 全链路，而 `smoke` 只做 `provider/status`、`bootstrap`、`health`、`reports/daily` 的轻量校验。因为前一轮虽然已经把这层顺序写进 `Mac-环境安装清单.md`，但完整 handoff 包里真正会被接手人第一眼看到的 `HANDOFF-README.md` 仍只是在平铺命令，没有直接提示“先 verify 再 start/smoke”。随后只做了最小验证：`bash -n scripts/package-mac-handoff.sh` 通过。本轮还没重新打包，所以这处顺序提示会在**下次**重新执行 `bash scripts/package-mac-handoff.sh` 后进入新归档。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的后端 staging 必带清单再补强一格，新增强制校验 `app/config/llm-profiles.example.json` 也必须在打包早期就被带进临时交接目录；因为这份 LLM 快切样板配置虽然之前已经在最终 `tar.gz` / `zip` 归档校验里被兜住，但 staging 阶段还没有和 `.env.example`、`README.md`、`server.js` 这些核心后端文件一起 fail-fast，出错时提示会更晚也更绕。随后只做了最小验证：`bash -n scripts/package-mac-handoff.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“第 4 步：启动后端”后面补了一行对 `handoff-backend.sh` 两个最关键动作的直白说明——明确 `verify = npm test + smoke-flow`，覆盖 `transcribe / create / train / archive / report` 全链路；`smoke` 则会轻量检查 `provider/status`、`bootstrap`、`health`、`reports/daily`，并顺手校验报告页关键字段。因为同一份清单前文虽然已经讲过 `mac-first-run.sh --backend-check` 的语义，但到了真正“先 verify 再 start/smoke”这一步，接手人仍可能只照抄命令，不知道这三个动作各自覆盖到哪。随后只做了最小验证：`grep -n "verify =\|smoke =\|reports/daily\|transcribe / create / train / archive / report" Mac-环境安装清单.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/config/llm-profiles.example.json` 已真实进包；这说明接手人拿到完整 handoff 包后，不只是能把后端跑起来，也已经带着一份可直接复制改名的 LLM 快切样板配置，不会到 Mac 上才发现 README 提到能切 provider、包里却缺示例文件。随后只做了最小验证：`tar -tzf dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz | rg '^chukouchengzhang-mac-handoff-20260405-001100/app/config/llm-profiles\.example\.json$'` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里 `scripts/mac-first-run.sh` 的 `--backend-check` 取值说明补齐到和当前脚本真实行为一致，新增明确 `auto / smoke / smoke-flow / test / verify / skip` 六种模式，以及 `smoke` 现在会顺手检查 `reports/daily` 与报告页关键字段；因为 README 和 `Mac-首编手册.md` 已经写清楚这层，但只看环境安装清单的 Mac 接手人之前仍要再翻脚本 `--help` 才知道怎么选。随后只做了最小验证：`grep -n 'backend-check\|reports/daily\|skip-backend' Mac-环境安装清单.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接检查最新 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260404-091106.tar.gz` 的内部文件，确认 `scripts/package-ios-app.sh` 与 `scripts/package-mac-handoff.sh` 都已真实进包；这说明即使接手人这一步只先拿 iOS-only 包去 Mac 做“先编译 / Mock / 看 UI”，后续若想在 Mac 上继续重打 iOS-only 包或完整 Mac handoff 包，这两个打包入口当前也没有在最新 iOS-only 成品里漏掉。随后只做了最小验证：`tar -tzf dist/chukouchengzhang-ios-app-20260404-091106.tar.gz | rg '^\./scripts/(package-ios-app\.sh|package-mac-handoff\.sh)$'` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `app/README.md` 里“下一步”第一条从过时的“接入真实音频上传 / STT”改成“补真实 Mac / iPhone 联调下的音频上传、局域网访问与本地 STT 依赖验证”；因为这条后端能力其实已经在 `app/src/server.js` 里落地（`/api/transcribe`、音频直传 `transcripts/create`、本地 STT 脚本链路都已存在），旧表述会把当前剩余工作误导成“后端还没做”，而项目眼下真正差的其实是 Mac / Xcode / 真机侧验证，不是接口能力本身。随后只做了最小验证：`grep -n "补真实 Mac / iPhone 联调下的音频上传、局域网访问与本地 STT 依赖验证" app/README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/smoke-backend.sh` 对 `GET /api/bootstrap` 的轻量契约校验再补强一格，新增要求 `bootstrap.providerStatus` 也必须显式包含 `ok / provider / checkedAt`；因为 iOS 客户端里的 `BootstrapDTO.providerStatus` 实际就是复用 `ProviderStatusDTO` 解码，而之前最轻量 smoke 虽然已经检查 `providerStatus` 是对象，却还没把这 3 个关键字段兜住，仍存在“`/api/provider/status` 单接口正常、但首屏聚合接口里的同名结构悄悄漂了，直到 App 真解码 `bootstrap` 才暴露”的缝。随后只做了最小验证：`bash -n scripts/smoke-backend.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接从 `ios-app/` 目录侧执行 `bash scripts/handoff-backend.sh smoke`，并在验证后执行 `bash scripts/handoff-backend.sh stop` 清理现场；结果轻量联调主路径真实通过了 `/api/provider/status`、`/api/bootstrap`、`/api/health`、`/api/reports/daily` 四条接口校验，其中刚在 `scripts/smoke-backend.sh` 新补的 `provider/status.provider` 约束也已经被当前真实后端返回满足。这说明“拿到 Mac 后先跑最轻量 smoke，看设置页 / 首屏 / 报告页依赖接口有没有漂”这条接手路径，目前不只脚本语法对，运行态也对。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `app/README.md` 里 `npm run verify` 的说明补齐到当前真实行为，明确它不只是“单测 + 烟测”，而是覆盖 `transcribe / create / train / archive / report` 全链路，并额外校验 iOS 客户端依赖字段；因为最近 `Mac-首编手册.md`、`Mac-编译失败排障.md`、`scripts/handoff-backend.sh --help`、`scripts/mac-first-run.sh --help` 都已对齐到这层真实覆盖范围，但后端 README 这里还留着一层偏泛的旧表述，拿到 Mac 后只翻 `app/README.md` 仍可能低估 verify 的价值。随后只做了最小验证：`grep -n "transcribe / create / train / archive / report" app/README.md` 命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/smoke-backend.sh` 对 `GET /api/provider/status` 的轻量契约校验再补强一格，新增要求返回里必须有 `provider`；因为 iOS 客户端里的 `ProviderStatusDTO` 实际要求 `ok / provider / checkedAt` 三个字段，而之前最轻量 smoke 只兜了 `ok / checkedAt`，仍存在“后端接口 200、轻量 smoke 通过，但 App 真到设置页或首屏解码时才因为少 `provider` 崩在 Mac/真机联调阶段”的缝。随后只做了最小验证：`bash -n scripts/smoke-backend.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/package-ios-app.sh` 的 iOS-only 归档 fail-fast 清单补齐，新增强制校验 `Config/Info.plist` 也必须真正进包；因为只带 `ios-app` 去 Mac 做“先编译 / Mock / 看 UI”时，这份 plist 仍然直接承载麦克风权限、局域网访问、ATS 与 `CKCZReleaseBaseURL`，之前完整 Mac handoff 包已经有这层兜底，但 iOS-only 包还没有，仍存在“源码侧检查通过、归档层误漏后到 Mac 才在 Xcode 编译或真机联调时暴露”的缝。随后只做了最小验证：`bash -n scripts/package-ios-app.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `ios-app/ChuKouChengZhang.xcodeproj/xcshareddata/xcschemes/ChuKouChengZhang.xcscheme` 已真实进包（归档内实际路径为 `chukouchengzhang-mac-handoff-20260405-001100/ios-app/ChuKouChengZhang.xcodeproj/xcshareddata/xcschemes/ChuKouChengZhang.xcscheme`）；这说明当前最新完整交接产物里，命令行 `xcodebuild` 烟测和 Xcode 打开共享 scheme 的入口文件没有在归档阶段丢失，距离“拿到 Mac 就编”又少一个静态交接暗雷。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `ios-app/scripts/mac-first-run.sh` 已真实进包（归档内实际路径为 `chukouchengzhang-mac-handoff-20260405-001100/ios-app/scripts/mac-first-run.sh`）；说明当前交给 Mac 接手人的完整产物，至少已经把“环境预检 → 静态校验 → 可选后端 verify → 签名写入 → xcodebuild 烟测”这条一键首编入口真实带上，不只停留在源码目录里。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/.env.example` 已真实进包（归档内实际路径为 `chukouchengzhang-mac-handoff-20260405-001100/app/.env.example`）；说明上一轮刚补进 `scripts/package-mac-handoff.sh` 的“环境样例文件不能漏带”这层 fail-fast，如今不只停留在源码规则，当前最新完整产物本身也已经满足。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `ios-app/Config/Info.plist` 已真实进包（归档内实际路径为 `chukouchengzhang-mac-handoff-20260405-001100/ios-app/Config/Info.plist`）；说明上一轮刚补进 `scripts/package-mac-handoff.sh` 的“Info.plist 不能漏带”这层 fail-fast，如今不只停留在源码规则，当前最新完整产物本身也已经满足。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 对完整 Mac 交接包的后端必带清单再补强一格，新增强制校验 `app/.env.example` 也必须真正进包；因为完整 handoff 包默认不会带真实 `.env` / `.env.local`，而 `app/.env.example` 正是接手人在 Mac 上补环境变量、切 provider 或本地 STT 时最自然的起点。之前脚本虽然会带 `app/` 目录，但最终归档层还没有单独 fail-fast 兜这件事，仍存在“后端能启动，但环境样例漏掉后要靠接手人自己猜配置”的缝。随后只做了最小验证：`bash -n scripts/package-mac-handoff.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 对完整 Mac 交接包的必带清单再补强一格，新增强制校验 `ios-app/Config/Info.plist` 也必须真正进包；因为这份 plist 直接承载麦克风权限、局域网访问、ATS 与 `CKCZReleaseBaseURL` 等 iOS 首编/联调关键配置，之前虽然源码侧 `check-ios-project.sh` 会检查它存在，但最终 handoff 归档层还没有单独 fail-fast 兜这件事，仍存在“源工程完整、归档误漏后到 Mac 才在 Xcode 编译阶段暴露”的缝。随后只做了最小验证：`bash -n scripts/package-mac-handoff.sh` 通过。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/src/load-env.js` 已真实进包；说明上一轮刚补进 `scripts/package-mac-handoff.sh` 的“后端入口依赖文件不能漏带”这层 fail-fast，如今不只停留在源码规则，当前最新完整产物本身也已经满足。当前最大阻塞点仍不是后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 对完整 Mac 交接包的后端必带清单再补强一格，新增强制校验 `app/src/load-env.js` 也必须真正进包；因为 `app/src/server.js` 启动时第一步就会 `require('./load-env')`，如果只盯 `server.js` 在不在，仍可能产出“主入口在、但一启动就缺依赖文件”的半残 handoff 包。当前最新完整产物仍是 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz`，这次还没重新打包验证；当前最大阻塞点仍不是后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/src/server.js` 已真实进包；说明上一轮刚补进 `scripts/package-mac-handoff.sh` 的“后端主入口不能漏带”这层 fail-fast，如今不只停留在源码规则，当前最新完整产物本身也已经满足。当前最大阻塞点仍不是后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 对完整 Mac 交接包的后端必带清单再补强一格，新增强制校验 `app/src/server.js` 也必须真正进包；因为当前完整 handoff 包就算 README、启动脚本和 smoke 脚本都在，只要后端主入口缺失，到 Mac 上依然会在真正 `npm start` / `bash scripts/start.sh` 时才暴露成“包看起来完整、实际上不能用”的暗雷。当前最大阻塞点仍不是后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 里的 `app/scripts/smoke-api-flow.sh`，确认最终产物中已真实带上 `/api/transcribe` 这层隔离总自检，以及 `text / language / duration / scene / inputSource / captureMeta / provider / segments` 这组 iOS 训练页语音转写依赖字段检查；说明现在不仅源码里的 `verify` 覆盖了语音转写契约，最终完整 handoff 包本身也已经带着这层校验。当前最大阻塞点仍不是后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 里的 `ios-app/scripts/smoke-backend.sh`，确认最终产物中已真实带上 `/api/reports/daily`、`assert_daily_report_contract`、`speechInputCount`、`bestSentence` 这层轻量契约检查；说明 README / 首编手册里强调的“轻量 smoke 也会顺手兜住报告页关键字段”，现在不只存在于源码，完整 handoff 包本身也一致。当前最大阻塞点仍不是后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/scripts/faster_whisper_transcribe.py` 已真实进包；说明上一轮刚补进 `package-mac-handoff.sh` 的 fail-fast 关注点，对应的本地 STT 核心转写脚本在当前最新完整交付物本身里也已经存在，不只是源码规则。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 里两处对 `verify` 的描述补齐到当前脚本真实行为，明确它不是泛泛的 `npm test + smoke-flow`，而是覆盖 `transcribe / create / train / archive / report` 全链路；这样接手人在 Mac 上遇到问题时，即使只翻排障文档，也不会低估这条后端总自检对语音转写与训练闭环的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/package-mac-handoff.sh` 对完整 Mac 交接包的后端必带清单再补强一格，新增强制校验 `app/scripts/faster_whisper_transcribe.py` 也必须真正进包；因为 `app/src/local-stt.js` 实际就是调用这支脚本做本地 STT，之前 fail-fast 只盯安装/预热脚本却没盯真正执行转写的脚本，存在“本地 STT 真正跑时才发现包里缺核心脚本”的交接暗雷。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接解包最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz`，并从包内 `ios-app/` 目录实际执行 `bash scripts/handoff-backend.sh verify` 通过；说明这份最终交付物本身（不是源工作区）当前已经能在解压后直接从 iOS 目录侧串起同级 `app/` 的 `npm test + smoke-api-flow`，连 `transcribe / create / train / archive / report` 这条后端总自检主路径也没有在打包后失效。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 本轮仅做 1 个小验证：直接检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 的内部文件，确认 `app/scripts/smoke-http-provider.sh`、`app/scripts/setup-local-stt.sh`、`app/scripts/faster_whisper_warmup.py` 都已真实进包；说明完整 handoff 包当前不只带了 iOS 工程和主后端，还带上了 README 里会直接用到的 smoke / 本地 STT 辅助脚本，Mac 接手后做后端自检或本地 STT 预热这条路没有再缺页。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境。
- 正式 iOS 宿主工程骨架：`ChuKouChengZhang.xcodeproj`
- `Info.plist`
- `Assets.xcassets` 与占位 App Icon
- 录音 / 上传 / 转写 / 优化 / 训练 / 灵感归档主流程源码
- 录音试听
- 结果页复制 / 分享 / 标签 / 下一步建议
- `/api/bootstrap` 聚合接口
- 后端测试：`npm test` 通过（6/6）
- 后端烟测：`/api/provider/status`、`/api/bootstrap`、`/api/health`、`/api/reports/daily` 实测通过
- 预检脚本：`scripts/check-ios-project.sh`
- 打包脚本：`scripts/package-ios-app.sh`
- 后端烟测脚本：`scripts/smoke-backend.sh`
- 一键写入 Team / Bundle Identifier 脚本：`scripts/configure-signing.sh`
- Xcode 命令行编译烟测脚本：`scripts/xcodebuild-smoke.sh`
- 真机局域网地址辅助脚本：`scripts/resolve-local-ip.sh`
- Mac 交接打包脚本：`scripts/package-mac-handoff.sh`（可把 `ios-app + app` 一起打包）
- Mac 交接后端统一入口：`scripts/handoff-backend.sh`（从 `ios-app` 目录侧统一启动 / 停止 / 烟测 / `npm test` / verify 同级 `app/` 后端）
- Mac 首编一键串联脚本：`scripts/mac-first-run.sh`（把静态校验、若同级 `app/` 存在则自动跑后端 verify=`npm test + smoke-flow`、签名写入、xcodebuild 模拟器烟测串成一条命令；只带 `ios-app` 时会自动跳过后端检查）
- `app/scripts/start.sh`、`status.sh`、`stop.sh`、`smoke-http-provider.sh` 已改成相对路径解析，不再绑死当前 Linux 工作区绝对路径，交接包放到任意 Mac 目录也能继续用
- 脚本调用方式已统一成 `bash ...`，避开 `sh`/dash 下 `pipefail` 直接报错
- Mac 首编手册：`Mac-首编手册.md`
- Mac 编译失败排障：`Mac-编译失败排障.md`
- 已明确区分“只编译/Mock 只带 ios-app”与“完整远端闭环需带同级 app/”两种交接方式，避免到 Mac 才发现后端没带过去
- 本轮再次执行后端测试：`npm test` 6/6 通过
- 已在当前 Linux 工作区实测 `bash scripts/handoff-backend.sh start/status/smoke/stop` 整套链路通过，说明新的 Mac 交接后端统一入口至少在非 Xcode 侧已经跑通
- 本轮已新增并实测 `bash scripts/mac-first-run.sh --help`；说明新的 Mac 首编一键串联脚本已接进交接链路
- 本轮已再次实测 `bash scripts/handoff-backend.sh smoke-flow` 通过；说明从 `ios-app` 目录侧触发同级 `app/` 后端完整 create/train/archive/report 链路仍然可用
- 新增 `app/package.json` 的 `npm run verify`，把 `npm test + bash scripts/smoke-api-flow.sh` 收敛成一个统一后端总自检入口
- `app/scripts/smoke-api-flow.sh` 已补成“接口契约烟测”，不只验证链路能跑，还会额外校验 `summaryTitle`、`suggestedTags`、`nextAction`、`provider`、`speechInputCount`、`bestSentence` 等 iOS 客户端首屏与结果页依赖字段
- `scripts/handoff-backend.sh` 已新增 `test` / `verify` 动作；`scripts/mac-first-run.sh` 默认后端检查从 `smoke-flow` 升级为 `verify`
- 本轮修掉了一个真实首编接手坑：`scripts/mac-first-run.sh` 之前即使没带同级 `app/` 也会默认硬跑后端检查，导致“只想先编译 / Mock”都被误拦住；现在改成 `--backend-check auto` 时会自动识别 `app/` 是否存在，有则跑 verify，无则明确提示并自动跳过，只保留 iOS 首编路径继续执行
- `README.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md`、`scripts/package-mac-handoff.sh` 生成的交接说明都已同步改成这套“iOS-only 自动跳过后端检查、完整交接包自动 verify”的新路径
- 本轮已再次实测：`bash scripts/mac-first-run.sh --help`、`bash scripts/check-ios-project.sh`、`cd ../app && npm run verify`、`bash scripts/package-mac-handoff.sh`；最新 Mac 交接包已更新并带上新的 HANDOFF-README 说明
- `README.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md`、`scripts/package-mac-handoff.sh` 生成的交接说明都已同步切到新的 `verify` 路径，Mac 接手时更不容易漏掉后端契约检查
- 新增 `scripts/mac-env-check.sh`：到 Mac 后可先查 `xcodebuild` / `xcrun` / `python3` / `curl` / `node` / `npm` 是否齐，并把结果写到 `build/mac-env-check.log`，把“打开 Xcode 才发现环境缺件”再往前挡一层
- `scripts/mac-first-run.sh` 已接入上述环境预检，首编路径现在从“静态校验起步”升级为“环境预检 → 静态校验 → 后端 verify（若同级 app/ 存在）→ 签名写入 → xcodebuild 烟测”
- `scripts/check-ios-project.sh` 已纳入 `scripts/mac-env-check.sh` 的存在性检查，Mac 交接脚本与 README / 首编手册 / 排障文档也都同步切到“先 env-check 再首编”的新路径
- `scripts/xcodebuild-smoke.sh` 现在除了 `build/xcodebuild-smoke.log` 之外，还会额外产出 `build/xcodebuild-smoke.xcresult`；首编失败时终于有结构化结果包可回传，而不只是纯文本日志
- 本轮已在当前 Linux 工作区再次实测：`bash ios-app/scripts/check-ios-project.sh`、`cd app && npm test`、`cd app && npm run verify` 全部通过；说明新增 env-check 接线与现有后端 verify 路径未被改坏
- 本轮仅做 1 个小验证：再次从 `ios-app` 目录侧执行 `bash scripts/handoff-backend.sh verify`，结果通过；说明给 Mac 接手人使用的统一后端入口当前仍能顺利串起 `npm test + smoke-api-flow`，交接链路这条主路径暂未回归
- 本轮仅做 1 个小改动：补齐了 `scripts/check-ios-project.sh` 的必检清单，新增把 `Mac-编译失败排障.md`、`scripts/package-ios-app.sh`、`scripts/smoke-backend.sh` 也纳入预检；这样接手人打包或拷目录到 Mac 前，更早就能发现“关键脚本/排障文档漏带了”这类低级交接坑
- 本轮仅做 1 个小改动：把 `app/README.md` 的后端接口清单补上 `GET /api/bootstrap`，并明确它就是 iOS 首屏依赖的聚合入口；这样拿到 Mac 做联调或排查时，不会只盯着 health/provider 而漏掉真正的首屏主接口
- 本轮仅做 1 个小验证：实际拉起同级 `app/` 后端并请求 `GET /api/bootstrap`，结果返回 200，且顶层 `providerStatus` / `transcripts` / `ideas` / `report` 四个 iOS 首屏依赖字段都在；说明最新文档里强调的首屏聚合入口当前没有回归
- 本轮仅做 1 个小改动：把 `app/README.md` 里仍残留的 `sh scripts/*.sh` 示例统一改成 `bash scripts/*.sh`（包含 `start/status/stop`、`smoke-http-provider.sh`、`setup-local-stt.sh`）；这样拿到 Mac 后按文档执行时，不会再踩到 `sh`/dash 对 `pipefail` 不兼容的老坑
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里后端手动启动示例从 `sh scripts/start.sh` 改成 `bash scripts/start.sh`；这样首编手册与当前交接文档主路径保持一致，接手人在 Mac 上照抄命令时不容易被旧写法带偏
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 里后端目录手动启动示例从 `sh scripts/start.sh` 改成 `bash scripts/start.sh`；这样排障手册与当前 README / 首编手册写法对齐，接手人在 Mac 上排障时不容易被旧命令示例带偏
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成的 `HANDOFF-README.md` 里残留的 `sh scripts/start.sh` 改成 `bash scripts/start.sh`；这样接手人即使直接照交接包内说明手动启后端，也不会再被旧命令示例带回 `sh`/dash 的坑里。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 也纳入 `scripts/check-ios-project.sh` 的必检清单；这样把工程拷到 Mac 或打包前，能更早发现“环境安装清单漏带了”这种真实交接坑。
- 本轮随改随验：执行 `bash scripts/check-ios-project.sh` 通过，新增的 `Mac-环境安装清单.md` 检查已经生效；当前仍存在的主要 WARN 只有 `DEVELOPMENT_TEAM` 尚未填写，这符合“到 Mac 再签名”的预期。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里本地 STT 安装示例从 `sh scripts/setup-local-stt.sh` 改成 `bash scripts/setup-local-stt.sh`；这样 Mac 接手人按清单安装 `faster-whisper` 时，不会再被旧的 `sh` 写法带回 shell 兼容性坑。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/handoff-backend.sh` 内部调用同级 `app/scripts/start.sh`、`status.sh`、`stop.sh` 的方式从 `sh` 统一改成 `bash`；这样 Mac 交接统一入口终于和 README / 首编手册 / 排障手册的命令约定彻底一致，也避免以后后端脚本一旦引入 bash 语法时，这条 handoff 路径悄悄回退。
- 本轮随改随验：执行 `bash scripts/handoff-backend.sh status` 返回“状态：未运行”，说明新的 `bash` 调用路径在当前工作区可正常打到同级 `app/` 后端脚本。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/package-mac-handoff.sh` 打包前的后端预检从 `npm test` 升级为 `npm run verify`，并同步修正文案；这样生成 Mac 交接包前，不只会测单元/接口测试，还会顺带跑 `smoke-api-flow` 的接口契约烟测，更接近“拿到 Mac 就能直接联调”的真实门槛。
- 本轮仅做 1 个小改动：把 `app/scripts/smoke-http-provider.sh` 内部调用同级 `start.sh` / `stop.sh` 的方式从 `sh` 统一改成 `bash`；这样这条后端 smoke 脚本不再绕回旧的 shell 兼容性坑，后续一旦同级脚本继续使用 bash 语法，Mac 接手人跑 http provider smoke 也不容易被暗雷绊住。
- 本轮仅做 1 个小改动：给 `ios-app/scripts/smoke-backend.sh` 补上了对 `GET /api/bootstrap` 顶层契约的轻量校验，要求返回里必须有 `providerStatus / transcripts / ideas / report`；这样 Mac 接手人跑最轻量 smoke 时，不再只是“接口 200 就算过”，而是更早发现首屏聚合接口结构被改坏。
- 本轮随改随验：实际拉起同级 `app/` 后端后执行 `bash scripts/smoke-backend.sh http://127.0.0.1:4321` 通过，并打印 `[OK] bootstrap 顶层字段已包含 providerStatus / transcripts / ideas / report`；说明这条更贴近 iOS 首屏依赖的轻量 smoke 当前可用。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/smoke-backend.sh` 从“只看接口能 200”再往前推半步，新增对 `GET /api/provider/status` 的 `ok / checkedAt` 和 `GET /api/health` 的 `ok / service / provider` 关键字段校验；这样 Mac 接手人跑最轻量 smoke 时，不只知道服务活着，也更早发现基础接口契约是否已经漂移。
- 本轮随改随验：直接对当前 4321 上已在运行的后端执行 `bash scripts/smoke-backend.sh http://127.0.0.1:4321` 通过，脚本已新增打印 `[OK] provider/status 已包含 ok / checkedAt` 与 `[OK] health 已包含 ok / service / provider`；说明这次轻量契约加固当前已落地可用。
- 本轮仅做 1 个小验证：从 `ios-app` 目录侧执行 `bash scripts/handoff-backend.sh smoke` 通过；统一入口先检查同级 `app/` 状态，再成功串起最近加固过的 `scripts/smoke-backend.sh`，并通过 `provider/status`、`bootstrap`、`health` 三条基础接口的轻量契约校验，说明“Mac 交接统一入口 → 轻量后端 smoke”这条接手路径当前没有回归。
- 本轮仅做 1 个小验证：再次从 `ios-app` 目录侧执行 `bash scripts/handoff-backend.sh verify` 通过；统一入口顺利串起 `npm test + smoke-api-flow`，且完整 create / train / archive / report 流程与 `bootstrap` 聚合结果中的 `summaryTitle`、`suggestedTags`、`nextAction`、`provider`、`stats.wordCount` 等 iOS 依赖字段都仍在，说明“Mac 交接统一入口 → 后端总自检”这条更重的接手路径当前也没有回归。
- 本轮仅做 1 个小改动：把 `scripts/xcodebuild-smoke.sh` 的默认 destination 从固定 `generic/platform=iOS Simulator` 改成“优先自动挑一台当前可用的 iPhone Simulator，挑不到再回退 generic”，并在输出里打印 `destinationSource`；这样拿到不同 Xcode / Simulator Runtime 的 Mac 时，命令行首编烟测更不容易因为默认目标过于笼统而增加排查成本。
- 本轮随改随验：执行 `bash -n scripts/xcodebuild-smoke.sh` 通过，说明这次脚本改动至少在语法层面没有引入新错误。
- 本轮仅做 1 个小改动：把 `README.md` 里对 `scripts/xcodebuild-smoke.sh` 的说明补齐，明确默认会优先自动挑一台当前可用的 iPhone Simulator，挑不到才回退到 `generic/platform=iOS Simulator`，并把实际来源打印到 `destinationSource`；这样接手人看到命令行烟测输出时，更容易理解脚本到底用了哪种目标，而不是还按旧印象把它当成固定 generic。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里 `scripts/xcodebuild-smoke.sh` 的说明也补齐到和当前脚本一致，明确默认会优先自动挑一台当前可用的 iPhone Simulator、挑不到才回退到 `generic/platform=iOS Simulator`，并打印 `destinationSource`；这样 Mac 接手人按首编手册操作时，不会只在 README 里看到新行为，而在首编主文档里还停留旧理解。
- 本轮仅做 1 个小改动：把 `STATUS.md` 也纳入 `scripts/check-ios-project.sh` 的必检清单；这样无论是打包前还是把目录拷到 Mac 前，都能更早发现“状态看板没带上、接手人缺少最新交接上下文”的问题，Mac 接手资料现在更完整。
- 本轮仅做 1 个小改动：把 `app/.env.example` 里本地 STT 安装示例从 `sh .../scripts/setup-local-stt.sh` 改成 `bash .../scripts/setup-local-stt.sh`；这样后端环境示例文件终于也和 README / Mac 环境清单 / 脚本实际写法一致，接手人在按 `.env.example` 对照配置本地 STT 时，不会再被残留旧命令带回 `sh`/dash 的兼容性坑。
- 本轮仅做 1 个小改动：修掉了 `ios-app/scripts/check-ios-project.sh` 的一个静态校验漏报坑——它原先用 `find ... | while ...` 管道遍历 Swift 文件，循环里一旦发现工程未引用源码，`fail=1` 可能只落在子 shell 里，脚本最终不一定真失败；现在改成进程替换 `while ...; done < <(find ... | sort)`，缺失引用时能正确把失败状态带到主脚本。
- 本轮随改随验：执行 `bash scripts/check-ios-project.sh` 通过；说明新遍历方式没有破坏现有预检路径，当前静态预检仍只剩 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/smoke-backend.sh` 对 `GET /api/bootstrap` 的轻量契约校验，从“只看顶层有 `providerStatus / transcripts / ideas / report`”补到“还要求 `report` 内包含 `totalWords / transcribeCount / trainingCount / polishCount / catchphraseCount / speechInputCount / bestSentence`”；这样 Mac 接手人在最轻量 smoke 阶段，就能更早发现报告页依赖字段被后端改坏。
- 本轮随改随验：执行 `bash scripts/handoff-backend.sh smoke` 通过；说明给 Mac 接手人使用的统一后端入口当前已能顺利通过增强后的 `bootstrap.report` 关键字段校验，这条最轻量联调路径暂未回归。
- 本轮仅做 1 个小改动：把 `app/README.md` 的后端接口清单补上了已在 `app/src/server.js` 暴露但文档里漏写的 `POST /api/polish`；这样拿到 Mac 后查后端接口时，文档终于和真实服务路由一致，不会误以为“只有 create 才能拿优化稿”。
- 本轮仅做 1 个小改动：把 `scripts/mac-env-check.sh` 里对 Xcode 环境的判断从“`xcodebuild` 命令存在”升级为“`xcodebuild -version` 真实可执行”；如果拿到 Mac 时 Xcode 还没完成首次初始化 / license 还没同意，预检现在会直接报 FAIL 并给出 `sudo xcodebuild -runFirstLaunch` 提示，不再把这种半就绪环境误判成可首编。
- 本轮随改随验：执行 `bash -n scripts/mac-env-check.sh` 通过；说明这次预检脚本改动至少在语法层面没有引入新错误。
- 本轮仅做 1 个小验证：用一条 Python 对照脚本核对了 `app/src/server.js` 与 `app/README.md` 的 `/api/*` 接口清单，结果 `GET/POST /api/bootstrap`、`/api/polish`、`/api/export`、`/api/import`、`/api/reset`、`/api/llm/*` 等 17 条当前真实路由与 README 完全一致，没有缺项也没有多写；说明拿到 Mac 后按 README 查后端接口做 iOS 联调，这一层当前不容易被过期文档带偏。
- 本轮仅做 1 个小改动：把 `scripts/xcodebuild-smoke.sh` 的 `project / scheme / configuration / destination / destinationSource / derivedData / resultBundle` 这些首编上下文，也一起写进 `build/xcodebuild-smoke.log`；这样以后 Mac 上命令行烟测失败时，回传日志文件本身就带完整目标信息，不用再额外口述“当时到底编到哪台模拟器、用了什么 destination”。
- 本轮仅做 1 个小改动：把 `app/README.md` 里本地 STT 安装示例从当前 Linux 工作区绝对路径改成在 `app/` 目录内可直接执行的相对命令（`bash scripts/setup-local-stt.sh`、`python3 scripts/faster_whisper_warmup.py`）；这样把交接包解压到任意 Mac 目录后，接手人照 README 安装本地 STT 时不会再被失效路径带偏。
- 本轮仅做 1 个小改动：把 `app/scripts/setup-local-stt.sh` 安装完成后的提示文案，从写死当前 Linux 工作区绝对路径，改成基于脚本实际所在目录回显 `$ROOT_DIR/.env.local`；这样把交接包解压到任意 Mac 目录后，接手人照脚本输出补 `CKCZ_STT_PYTHON` 时不会再被旧路径误导。已执行 `sh -n app/scripts/setup-local-stt.sh`，语法通过。
- 本轮仅做 1 个小改动：把 `app/.env.example` 里本地 STT 安装提示从当前 Linux 工作区绝对路径改成“在 `app/` 目录内执行 `bash scripts/setup-local-stt.sh`”；这样把交接包解压到任意 Mac 目录后，接手人按环境样例配置本地 STT 时，不会再被失效路径带偏。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/smoke-backend.sh` 对 `GET /api/bootstrap` 的轻量契约校验再收紧一格，新增要求 `providerStatus` 必须是 JSON 对象，`transcripts` / `ideas` 必须是 JSON 数组；这样 Mac 接手人跑最轻量 smoke 时，不只知道字段名还在，也能更早发现首屏聚合接口把关键类型改坏了。
- 本轮随改随验：执行 `bash scripts/handoff-backend.sh smoke` 通过；说明给 Mac 接手人使用的统一后端入口当前已能顺利通过增强后的 `bootstrap` 类型校验，`provider/status`、`bootstrap`、`health` 这条最轻量联调路径仍然可用。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里“拿到 Mac 后”的首编命令从当前 Linux 工作区绝对路径改成解压后可直接执行的相对命令（`bash scripts/mac-env-check.sh`、`bash scripts/mac-first-run.sh`、`bash scripts/configure-signing.sh`、`bash scripts/xcodebuild-smoke.sh`、`bash scripts/resolve-local-ip.sh`）；这样接手人到 Mac 后不必先把文档里的源环境路径手改一遍，首编手册终于和交接包/HANDOFF-README 的相对路径用法对齐。
- 本轮仅做 1 个小改动：把 `README.md` 里“到 Mac 后”的首编相关命令也从当前 Linux 工作区绝对路径统一改成解压后可直接执行的相对命令（`bash scripts/mac-env-check.sh`、`bash scripts/mac-first-run.sh`、`bash scripts/configure-signing.sh`、`bash scripts/xcodebuild-smoke.sh`、`bash scripts/resolve-local-ip.sh`）；这样 README 终于和 `Mac-首编手册.md`、交接包里的 `HANDOFF-README.md` 对齐，接手人不会一边看 README 一边又被源环境路径带偏。
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 里“到 Mac 后执行”的 `mac-env-check`、`mac-first-run`、`configure-signing`、`xcodebuild-smoke`、`resolve-local-ip` 命令与 `build/` 日志路径，也统一改成解压后可直接照抄的相对写法；这样排障主文档终于和 `README.md`、`Mac-首编手册.md`、交接包里的 `HANDOFF-README.md` 对齐，接手人到 Mac 上排错时不必再先把源环境绝对路径手改一遍。
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 开头“补救打包同级 app/ 后端”的示例，从源环境绝对路径改成“先进入 `ios-app/` 目录，再执行 `bash scripts/package-mac-handoff.sh`”；这样排障文档里关于“重新打完整交接包”的动作也终于和当前相对路径交接口径一致，不会再把接手人带回源工作区路径。
- 本轮仅做 1 个小改动：把 `app/README.md` 顶部最常用的后端启动 / 后台运行 / smoke / test / verify 命令与 `.env`、日志示例，从源环境绝对路径改成基于交接包的相对写法（`cd app`、`app/.env`、`app/runtime/app.log`）；这样把完整交接包解压到任意 Mac 目录后，接手人照后端 README 操作时，不会再被源环境路径带偏。
- 本轮仅做 1 个小改动：把 `app/README.md` 里剩余的后端实现/配置文件引用，以及 LLM profiles 的复制命令与目标文件路径，从源环境绝对路径统一改成 `src/...`、`config/...`、`scripts/...` 这类可直接在 `app/` 目录照抄的相对写法；这样拿到 Mac 后查看 provider/STT/LLM 配置或执行 `cp config/llm-profiles.example.json config/llm-profiles.json` 时，不会再被当前 Linux 工作区路径带偏。
- 本轮仅做 1 个小改动：把 `ios-app/README.md` 里仍指向源 Linux 工作区的后端启动、Mac 交接打包、统一后端入口和 `xcodebuild` 烟测结果路径示例，统一改成解压后可直接照抄的相对写法（如 `cd ../app`、`bash scripts/...`、`build/...`）；这样接手人拿到 Mac 后不必再先手改命令里的源环境路径。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里仍残留的源环境绝对路径示例再收一刀——将“原环境打完整 Mac 交接包”的命令改成先进入 `ios-app/` 后执行 `bash scripts/package-mac-handoff.sh`，并把 `xcodebuild` 烟测产物路径改成 `build/xcodebuild-smoke.log` / `build/xcodebuild-smoke.xcresult`；这样首编主手册终于也和交接包内实际目录结构对齐，接手人到 Mac 后更容易直接照抄，不会再被源环境路径带偏。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里 `mac-env-check.sh` 的日志输出路径从 `ios-app/build/mac-env-check.log` 改成脚本真实写入的 `build/mac-env-check.log`；这样接手人已经进入 `ios-app/` 目录后，不会再被一个多余的 `ios-app/` 前缀带偏，找日志和回传排障信息会更直接。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成的 `HANDOFF-README.md` 统一成“先进入 `ios-app/`，再执行 `bash scripts/...`”的相对写法，并把首编日志路径同步改成 `build/...`；这样接手人直接照交接包内说明操作时，不会再遇到和 `README.md` / `Mac-首编手册.md` / `Mac-编译失败排障.md` 口径不一致的命令与路径示例。
- 本轮仅做 1 个小改动：把 `scripts/handoff-backend.sh` 自带的 `--help` / 用法示例，也统一成“先进入 `ios-app/`，再执行 `bash scripts/handoff-backend.sh ...`”的相对写法；这样接手人即使不看 README、只直接跑脚本看帮助，也不会再看到和当前交接文档不一致的命令口径。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里仍残留的 `bash ios-app/scripts/handoff-backend.sh ...` 三处示例，统一改成“先进入 `ios-app/` 后执行 `bash scripts/handoff-backend.sh ...`”；这样首编主手册里的后端联调命令终于和 `README.md`、脚本 `--help`、交接包 `HANDOFF-README.md` 完全一致，接手人不会在 Mac 上照着主手册又多打一层 `ios-app/` 路径。
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 里还残留的 `bash ios-app/scripts/handoff-backend.sh verify/start/smoke` 三处示例，统一改成“先进入 `ios-app/` 后执行 `bash scripts/handoff-backend.sh ...`”；这样排障主文档终于也和 `README.md`、`Mac-首编手册.md`、脚本 `--help`、交接包 `HANDOFF-README.md` 完全对齐，接手人在 Mac 上排障时不会再被多余的 `ios-app/` 路径带偏。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里“如果你更想直接进后端目录”这段示例从 `cd app` 改成 `cd ../app`；因为这份手册前文已明确让接手人先进入 `ios-app/` 目录，旧写法会把人带去不存在的 `ios-app/app`。现在首编主手册里的手动进后端路径终于和 `README.md` 的相对目录关系对齐。
- 本轮仅做 1 个小改动：把 `Mac-编译失败排障.md` 里“如果你更想直接进后端目录”这段示例从 `cd app` 改成 `cd ../app`；因为这份排障文档默认语境同样是“你已经在 `ios-app/` 目录里”，旧写法会把接手人带去不存在的 `ios-app/app`。这样排障文档里的手动进后端路径终于也和 `README.md`、`Mac-首编手册.md`、当前交接包目录结构对齐。
- 本轮仅做 1 个小验证：实际检查最新 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260401-180322.tar.gz` 的内部文件，确认 `HANDOFF-README.md`、Xcode 工程、首编脚本和同级 `app/` 后端都已打入，但 `ios-app/Mac-环境安装清单.md` 当前没有被打进包里；说明“交接包资料完整性”这条链路还有一个真实漏项，最新归档还不能算完全自洽。
- 本轮仅做 1 个小改动：给 `scripts/package-mac-handoff.sh` 补了一个显式 fail-fast 检查——如果 `ios-app/Mac-环境安装清单.md` 没有进入打包结果，脚本会直接报错退出，不再悄悄产出一个资料缺页的 Mac 交接包；这样后续重新打包时，至少不会再把同类漏项静默带到 Mac 侧。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新归档 `dist/chukouchengzhang-mac-handoff-20260402-201621.tar.gz`；按当前 fail-fast 逻辑，这也说明 `Mac-环境安装清单.md` 已成功进入包内。不过这次验证同时暴露出一个新的工程级小坑：脚本生成 `HANDOFF-README.md` 时 heredoc 里未转义的反引号触发了 shell 命令替换，终端出现 `ios-app/: No such file or directory`，且归档里首段“先进入 \`ios-app/\` 目录”那行文字被吃掉了，说明最新交接包说明文本还不算完全干净。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 里生成 `HANDOFF-README.md` 首段的 heredoc 反引号改成转义写法，避免 shell 在打包时把 Markdown 行内代码误当成命令替换；这样脚本层面的 README 生成坑已经收住，但还没重新打包验证，所以当前最新归档仍不能算已修复。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新归档 `dist/chukouchengzhang-mac-handoff-20260402-211610.tar.gz`；随后直接解包检查 `HANDOFF-README.md` 首段，已恢复正常显示“先进入 `ios-app/` 目录”，打包过程也未再出现 `ios-app/: No such file or directory`。说明交接包 README 生成的小坑这次已经在真实产物上闭环验证通过。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“当前最新交接包 / 最建议下载的包 / 同时下载到本地”的 Mac 交接包文件名，从旧的 `chukouchengzhang-mac-handoff-20260401-180322.tar.gz` 统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260402-211610.tar.gz`，并顺手确认对应 `.sha256` 文件存在；这样接手人按环境安装清单拿包时，不会再被已经过期的旧归档名带偏。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的交接包 fail-fast 检查，从只盯 `Mac-环境安装清单.md`，补成同时要求 `Mac-首编手册.md`、`Mac-编译失败排障.md` 也必须进包；这样以后就算 copy/exclude 逻辑再漂，也不会静默产出“有工程、少关键交接文档”的半残交接包。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新归档 `dist/chukouchengzhang-mac-handoff-20260402-224548.tar.gz`；由于脚本现已在真实打包流程里 fail-fast 校验 `Mac-环境安装清单.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md` 必须进包，而本次打包未触发拦截并正常完成，也说明这三份关键交接文档当前都已随最新 Mac 交接包一并产出。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍残留的“当前最新交接包 / 最建议下载的包 / 同时下载到本地”文件名，从旧的 `chukouchengzhang-mac-handoff-20260402-211610.tar.gz` 统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260402-224548.tar.gz`，并确认对应 `.sha256` 文件存在；这样接手人按环境清单拿包时，不会再被上一个归档名带偏。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的交接包 fail-fast 检查从三份 Mac 文档再补到 `STATUS.md`；这样以后重新打包时，如果状态看板没进包，脚本会直接报错退出，不再静默产出“有工程但缺最新接手上下文”的 Mac 交接包。随后已执行 `bash -n scripts/package-mac-handoff.sh`，语法通过。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新交接包 `dist/chukouchengzhang-mac-handoff-20260403-001601.tar.gz`；由于脚本现已在真实打包流程里先跑 `npm run verify`，并对 `STATUS.md`、`Mac-环境安装清单.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md` 做 fail-fast 检查，而本次打包未触发拦截并正常完成，说明“最新状态看板 + 三份关键 Mac 文档”当前都已随最新交接包一并产出。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版归档 `chukouchengzhang-mac-handoff-20260402-224548.tar.gz` 的“当前最新交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处引用，统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260403-001601.tar.gz`（其 `.sha256` 文件已存在于 `dist/`）；这样接手人按环境清单下载时，不会再被过期交接包名带偏。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-011652.tar.gz`；静态预检与工程接线校验全部通过，当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN。这样即使只拿 `ios-app` 去 Mac 做“先编译 / Mock / 看 UI”，接手人拿到的也不再是 4 月 1 日的旧包，而是包含最新脚本、文档和状态看板的新归档。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版 iOS-only 归档 `chukouchengzhang-ios-app-20260401-145608.tar.gz` 的下载示例，更新到当前真实最新产物 `chukouchengzhang-ios-app-20260403-011652.tar.gz`，并确认对应 `.sha256` 文件存在；这样只想先把 `ios-app` 拿到 Mac 编译 / Mock 的接手人，不会再被过期包名带偏。
- 本轮仅做 1 个小验证：实际检查最新 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-011652.tar.gz` 的内部文件，确认 `STATUS.md`、`README.md`、`Mac-环境安装清单.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md`、`scripts/mac-env-check.sh`、`scripts/mac-first-run.sh`、`scripts/check-ios-project.sh`、`scripts/xcodebuild-smoke.sh` 与 `ChuKouChengZhang.xcodeproj/project.pbxproj` 都已打进包里；说明“只带 ios-app 去 Mac 先编译 / Mock / 看 UI”这条交接路径当前资料并未缺页。
- 本轮仅做 1 个小改动：给 `scripts/mac-first-run.sh` 补了 `--backend-check` 参数白名单校验，只接受 `auto / smoke / smoke-flow / test / verify / skip`；这样接手人在 Mac 上首编时如果把后端检查动作拼错，不会等到脚本跑到半程才被 `handoff-backend.sh` 的未知动作报错打回，而是会在入口处立刻得到清晰提示。
- 本轮随改随验：执行 `bash -n scripts/mac-first-run.sh` 通过；说明这次脚本加固至少在语法层面没有引入新问题。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 补上 `scripts/mac-first-run.sh --backend-check` 的全部合法取值（`auto / smoke / smoke-flow / test / verify / skip`）及对应含义，并明确 `skip` 等价于 `--skip-backend`；这样接手人到 Mac 上不必先跑 `--help` 猜参数，也更不容易把首编流程卡在一个拼错的后端检查动作上。
- 本轮仅做 1 个小改动：把 `ios-app/README.md` 里 `scripts/mac-first-run.sh` 的说明也补上 `--backend-check` 的全部合法取值与用途（`auto / smoke / smoke-flow / test / verify / skip`）；这样接手人即使只先看 README，也能直接决定“只编译 / Mock 先跳过后端”还是“带同级 app/ 时跑 verify”，不用再翻脚本 `--help` 或首编手册。
- 本轮仅做 1 个小改动：把 `scripts/mac-first-run.sh --help` 文案里漏掉的 `--backend-check skip` 补上，并明确它等价于 `--skip-backend`；这样脚本自解释能力终于和实际支持的参数集一致，接手人即使只看 `--help`，也不会误以为 `skip` 不是合法取值。随后已执行 `bash scripts/mac-first-run.sh --help | grep -F -- '--backend-check skip'`，输出命中，说明帮助文案已更新到位。
- 本轮仅做 1 个小改动：给 `scripts/package-ios-app.sh` 补了归档内容 fail-fast 检查，打完 iOS-only 包后会显式确认 `STATUS.md`、`README.md`、三份 Mac 文档、`scripts/mac-env-check.sh`、`scripts/mac-first-run.sh`、`scripts/check-ios-project.sh`、`scripts/xcodebuild-smoke.sh` 与 `ChuKouChengZhang.xcodeproj/project.pbxproj` 都真的进了 tar.gz；这样以后重新打 iOS-only 包时，不必再靠人工解包抽查，能更早拦住“给 Mac 的包能编译但交接资料缺页”的问题。
- 本轮仅做 1 个小验证：实际执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-051559.tar.gz`；由于脚本现在会在真实打包流程里对 `STATUS.md`、`README.md`、三份 Mac 文档、`scripts/mac-env-check.sh`、`scripts/mac-first-run.sh`、`scripts/check-ios-project.sh`、`scripts/xcodebuild-smoke.sh` 与 `ChuKouChengZhang.xcodeproj/project.pbxproj` 做 fail-fast 检查，而本次打包未触发拦截并正常完成，说明这套新的 iOS-only 归档完整性校验已经在真实产物上闭环验证通过。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到 Mac 侧再签名。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“只想先编译 App / 看 UI”对应的 iOS-only 下载包文件名，从过期的 `chukouchengzhang-ios-app-20260403-011652.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-ios-app-20260403-051559.tar.gz`；这样接手人按环境清单下载“只带 ios-app”的包时，不会再被上一版归档名带偏。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的归档 fail-fast 检查再补强一格，新增强制校验 `ios-app/README.md`、`scripts/check-ios-project.sh`、`scripts/mac-env-check.sh`、`scripts/mac-first-run.sh`、`scripts/configure-signing.sh`、`scripts/xcodebuild-smoke.sh`、`scripts/resolve-local-ip.sh` 与 `ChuKouChengZhang.xcodeproj/project.pbxproj` 都必须真正进包；这样后续重新打 Mac 交接包时，不只会拦“文档缺页”，也能更早拦住“首编关键脚本或工程文件漏带”的半残交接包。注意：本轮未重新打包，因此这次是脚本层推进，不是新归档验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新交接包 `dist/chukouchengzhang-mac-handoff-20260403-064612.tar.gz`；由于脚本现已在真实打包流程里先跑 `npm run verify`，并对 `STATUS.md`、三份 Mac 文档、`ios-app/README.md`、`scripts/check-ios-project.sh`、`scripts/mac-env-check.sh`、`scripts/mac-first-run.sh`、`scripts/configure-signing.sh`、`scripts/xcodebuild-smoke.sh`、`scripts/resolve-local-ip.sh` 与 `ChuKouChengZhang.xcodeproj/project.pbxproj` 做 fail-fast 检查，而本次打包未触发拦截并正常完成，说明这批“Mac 首编关键文档 + 脚本 + 工程文件”当前都已随最新完整交接包一并产出。
- 本轮仅做 1 个小改动：统一把完整 Mac 交接包的当前推荐文件名从旧的 `chukouchengzhang-mac-handoff-20260403-001601.tar.gz` 更正为真实最新产物 `chukouchengzhang-mac-handoff-20260403-064612.tar.gz`——已同步到 `Mac-环境安装清单.md` 的下载指引，并把本文件“下一步”里使用的包名也一起改正；这样接手人按最新文档执行时，不会再被上一版归档名带偏。
- 本轮仅做 1 个小验证：实际检查最新 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-051559.tar.gz` 的内部文件，确认 `scripts/configure-signing.sh` 与 `scripts/resolve-local-ip.sh` 都已打进包里；说明“只带 ios-app 去 Mac 先编译 / Mock，后续再补签名或真机局域网联调”这条接手路径当前没有缺这两个关键脚本。
- 本轮仅做 1 个小改动：把 `scripts/package-ios-app.sh` 的 iOS-only 归档 fail-fast 检查再补强一格，新增强制校验 `scripts/configure-signing.sh` 与 `scripts/resolve-local-ip.sh` 必须真正进包；这样以后重新打“只带 ios-app 去 Mac 先编译 / Mock”的归档时，就算 copy/exclude 逻辑漂了，也会更早拦住“签名脚本或真机局域网辅助脚本漏带”的半残交接包。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-091112.tar.gz`；静态预检、Xcode 工程接线校验与归档 fail-fast 检查全部通过，说明这次新增的 `scripts/configure-signing.sh` 与 `scripts/resolve-local-ip.sh` 进包校验已经在真实打包流程上闭环验证通过。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到真实 Mac 侧再签名。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“只想先编译 App / 看 UI”对应的 iOS-only 下载包文件名，从过期的 `chukouchengzhang-ios-app-20260403-051559.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-ios-app-20260403-091112.tar.gz`，并确认对应 `.sha256` 文件存在；这样接手人按环境清单下载 iOS-only 包时，不会再被上一版归档名带偏。
- 本轮仅做 1 个小验证：用一条 Python 对照脚本再次核对了 `app/src/server.js` 与 `app/README.md` 的 `/api/*` 接口清单，结果当前 17 条真实路由与 README 完全一致，`MISSING_IN_README` / `EXTRA_IN_README` 都为空；说明拿到 Mac 后按 README 查后端接口做 iOS 联调，这层接口文档当前没有回归。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的归档 fail-fast 检查再补强一格，新增强制校验 `ChuKouChengZhang.xcodeproj/xcshareddata/xcschemes/ChuKouChengZhang.xcscheme` 也必须真正进包；这样以后重新打完整 Mac 交接包时，如果共享 scheme 漏带，脚本会直接拦下，不再静默产出“工程在但命令行 xcodebuild 入口缺失”的半残交接包。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `scripts/package-ios-app.sh` 的 iOS-only 归档 fail-fast 检查也补上了 `ChuKouChengZhang.xcodeproj/xcshareddata/xcschemes/ChuKouChengZhang.xcscheme`；这样以后只带 `ios-app` 去 Mac 做首编 / Mock 时，如果共享 scheme 漏带，脚本也会直接拦下，不再静默产出“工程在但命令行 xcodebuild 入口缺失”的半残 iOS-only 归档。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：实际检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260403-064612.tar.gz` 的内部文件，确认 `ios-app/ChuKouChengZhang.xcodeproj/xcshareddata/xcschemes/ChuKouChengZhang.xcscheme` 已真实进包；说明“拿到 Mac 后从命令行直接跑 xcodebuild / xcodebuild-smoke”这条入口当前没有因为共享 scheme 漏带而在交接包层面先天失效。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-131103.tar.gz`；由于脚本现已在真实打包流程里对 `STATUS.md`、`README.md`、三份 Mac 文档、`scripts/mac-env-check.sh`、`scripts/mac-first-run.sh`、`scripts/check-ios-project.sh`、`scripts/configure-signing.sh`、`scripts/resolve-local-ip.sh`、`scripts/xcodebuild-smoke.sh`、`ChuKouChengZhang.xcodeproj/project.pbxproj` 与 `ChuKouChengZhang.xcodeproj/xcshareddata/xcschemes/ChuKouChengZhang.xcscheme` 做 fail-fast 检查，而本次打包未触发拦截并正常完成，说明 iOS-only 交接包里“共享 scheme 不能漏带”这道新闸已经在真实产物上闭环验证通过。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到真实 Mac 侧再签名。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“只想先编译 App / 看 UI”对应的 iOS-only 下载包文件名，从过期的 `chukouchengzhang-ios-app-20260403-091112.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-ios-app-20260403-131103.tar.gz`；并已顺手核对 `.sha256` 文件存在。这样接手人按环境清单下载 iOS-only 包时，不会再被上一版归档名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：给 `scripts/package-mac-handoff.sh` 再补一层交付完整性闸门——脚本现在会在打包后显式确认 `HANDOFF-README.md` 已生成且已真正进入最终 `tar.gz`；这样即使以后再出现 README 生成或归档阶段的静默异常，也不会产出“包在，但接手说明丢了”的完整 Mac 交接包。随后已执行 `bash -n scripts/package-mac-handoff.sh`，语法通过。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新交接包 `dist/chukouchengzhang-mac-handoff-20260403-144110.tar.gz`；由于脚本现已在真实打包流程里先跑 `npm run verify`，并且新增的 `HANDOFF-README.md` 生成 + 进包 fail-fast 检查未触发拦截，说明这层“交接说明不能丢”的闸门已经在真实产物上闭环验证通过。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版完整 Mac 交接包 `chukouchengzhang-mac-handoff-20260403-064612.tar.gz` 的“当前最新交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处引用，统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260403-144110.tar.gz`；这样接手人按环境清单拿包时，不会再被上一版归档名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：用一条 Python 扫描 `README.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md`、`Mac-环境安装清单.md` 与 `scripts/*.sh` 中出现的交接包文件名，结果当前手递给 Mac 接手人的文档里只剩 `Mac-环境安装清单.md` 在引用最新真实产物（完整包 `chukouchengzhang-mac-handoff-20260403-144110.tar.gz`、iOS-only 包 `chukouchengzhang-ios-app-20260403-131103.tar.gz`），脚本里也仅保留按时间戳生成新包名的模板；说明当前对外文档与打包脚本这一层没有残留旧归档名再把接手人带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：用一条 Python 对照脚本再次核对了 `app/src/server.js` 与 `app/README.md` 的 `/api/*` 接口清单，结果当前 17 条真实路由与 README 完全一致，`MISSING_IN_README` / `EXTRA_IN_README` 都为空；说明拿到 Mac 后按后端 README 查接口做 iOS 联调，这一层当前没有回归。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：实际从 `ios-app` 目录侧拉起同级 `app/` 后端并请求 `GET /api/reports/daily`，结果返回 200，且 `totalWords / transcribeCount / trainingCount / polishCount / catchphraseCount / speechInputCount / bestSentence` 这 7 个 iOS 首页/报告页直接依赖字段都在；说明除了已反复校验过的 `bootstrap.report` 之外，App 单独调用的报告接口当前也没有回归。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `scripts/package-ios-app.sh` 的 iOS-only 归档 fail-fast 检查再补强一格，新增强制校验 `scripts/handoff-backend.sh` 与 `scripts/smoke-backend.sh` 也必须真正进包；这样以后就算 copy/exclude 逻辑再漂，交给 Mac 接手人的 iOS-only 包也不会静默缺掉两条后续联调常用脚本。
- 本轮随改随验：执行 `bash -n scripts/package-ios-app.sh` 通过；说明这次小改动至少在脚本语法层面没有引入新问题。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-181112.tar.gz`；由于脚本现已在真实打包流程里对 `scripts/handoff-backend.sh` 与 `scripts/smoke-backend.sh` 也做 fail-fast 检查，而本次打包未触发拦截并正常完成，说明 iOS-only 交接包里“后续联调常用脚本不能漏带”这道新闸已经在真实产物上闭环验证通过。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到真实 Mac 侧再签名。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“只想先编译 App / 看 UI”对应的 iOS-only 下载包文件名，从过期的 `chukouchengzhang-ios-app-20260403-131103.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-ios-app-20260403-181112.tar.gz`；这样接手人按环境清单下载 iOS-only 包时，不会再被上一版归档名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的归档 fail-fast 检查再补强一格，新增强制校验 `scripts/package-ios-app.sh` 与 `scripts/package-mac-handoff.sh` 这两个打包脚本本身也必须真正进包；这样接手人拿到完整 Mac 交接包后，如果在 Mac 上继续迭代并想重新打 iOS-only 或完整 handoff 包，不会遇到“工程在、但打包脚本漏带了”的低级交接坑。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `scripts/package-ios-app.sh` 的 iOS-only 归档 fail-fast 检查再补强一格，新增要求 `scripts/package-ios-app.sh` 与 `scripts/package-mac-handoff.sh` 这两个打包脚本本身也必须真正进包；这样即使接手人只拿 `ios-app` 去 Mac 先编译 / Mock，后续想在 Mac 上继续重打 iOS-only 包或完整 handoff 包，也不会碰到“工程在、但打包脚本自己漏带了”的低级交接坑。本轮还没重新打包验证，所以当前最新 iOS-only 归档仍是 `chukouchengzhang-ios-app-20260403-181112.tar.gz`。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-201144.tar.gz`；由于脚本现已在真实打包流程里把 `scripts/package-ios-app.sh` 与 `scripts/package-mac-handoff.sh` 也纳入 fail-fast 检查，而本次打包未触发拦截并正常完成，说明“iOS-only 包里连重打包脚本自己都不能漏带”这道新闸已经在真实产物上闭环验证通过。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到真实 Mac 侧再签名。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“只想先编译 App / 看 UI”对应的 iOS-only 下载包文件名，从过期的 `chukouchengzhang-ios-app-20260403-181112.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-ios-app-20260403-201144.tar.gz`；这样接手人按环境清单下载 iOS-only 包时，不会再被上一版归档名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的最终归档校验从“只确认 `HANDOFF-README.md` 在 tar.gz 里”补强为“连 `STATUS.md`、三份 Mac 文档、首编/签名/联调脚本、`project.pbxproj`、共享 `xcscheme`，以及带后端时的 `app/package.json` 也要在最终 tar.gz 里逐一命中”；这样即使 staging 阶段文件都在，真正产出的 Mac 交接包如果在归档环节漏了关键首编资料，也会直接 fail-fast，不再把半残包静默交给 Mac 接手人。随手执行了 `bash -n scripts/package-mac-handoff.sh`，语法通过；当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新交接包 `dist/chukouchengzhang-mac-handoff-20260403-214248.tar.gz`；由于脚本现已在真实打包流程里先跑 `npm run verify`，并对最终 `tar.gz` 逐一核对 `HANDOFF-README.md`、`STATUS.md`、三份 Mac 文档、首编/签名/联调脚本、`project.pbxproj`、共享 `xcscheme` 以及 `app/package.json` 是否真实进包，而本次打包未触发拦截并正常完成，说明这层“最终归档不能漏关键接手资料”的闸门已经在真实产物上闭环验证通过。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版完整 Mac 交接包 `chukouchengzhang-mac-handoff-20260403-144110.tar.gz` 的“当前最新交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处引用，统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260403-214248.tar.gz`，并已核对对应 `.sha256` 文件存在；这样接手人按环境清单拿包时，不会再被上一版归档名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：实际检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260403-214248.tar.gz` 的内部文件，确认 `ios-app/scripts/package-ios-app.sh` 与 `ios-app/scripts/package-mac-handoff.sh` 都已真实进包；说明接手人拿到完整包后，不仅能首编/联调，也还能在 Mac 侧继续重打 iOS-only 或完整 handoff 包，这两个打包入口当前没有在最终交付物里漏掉。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。

- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的最终归档校验从“只盯 tar.gz”补到“zip 也逐项命中校验”——脚本现在在生成 zip 后，也会显式核对 `HANDOFF-README.md`、`STATUS.md`、三份 Mac 文档、首编/签名/联调脚本、`project.pbxproj`、共享 `xcscheme` 以及带后端时的 `app/package.json` 是否真实进 zip，不再只对 tar.gz 有闸门；这样接手人如果拿的是顺手生成的 zip 包，也不容易撞上“tar.gz 没问题、zip 却静默缺文件”的交付暗雷。随后已执行 `bash -n scripts/package-mac-handoff.sh`，语法通过。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新交接包 `dist/chukouchengzhang-mac-handoff-20260403-234058.tar.gz`；脚本先跑 `npm run verify`，随后完整走通了最终 `tar.gz` 的逐项命中校验，说明完整 Mac 交接主路径当前仍可真实产出可交付归档。当前环境没有 `zip` 命令，所以这次还没法实机补验 zip 分支；但距离“拿到 Mac 就能直接接手”的最大阻塞点依旧不是打包脚本，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版完整 Mac 交接包 `chukouchengzhang-mac-handoff-20260403-214248.tar.gz` 的“当前最新交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处引用，统一同步到当前真实最新产物 `chukouchengzhang-mac-handoff-20260403-234058.tar.gz`，并已确认对应 `.sha256` 文件存在；这样接手人按环境清单拿包时，不会再被过期交接包名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小改动：把 `scripts/package-ios-app.sh` 的 ZIP 分支也补上最终归档逐项命中校验；现在只要运行环境存在 `zip` 命令，脚本除了校验 tar.gz，还会逐一确认 `STATUS.md`、三份 Mac 文档、首编/签名/联调脚本、`project.pbxproj` 与共享 `xcscheme` 也真实进了 ZIP，不再出现“tar.gz 没问题、iOS-only 的 zip 却静默缺文件”的盲区。当前还没在有 `zip` 的环境里重打 iOS-only 包，所以这次属于脚本层推进；最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：用一条 Python 扫描 `STATUS.md`、`README.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md`、`Mac-环境安装清单.md` 里的交接包文件名，结果当前对外交付口径里只有 `Mac-环境安装清单.md` 仍显式引用包名，而且它已统一指向最新真实产物——完整包 `chukouchengzhang-mac-handoff-20260403-234058.tar.gz` 与 iOS-only 包 `chukouchengzhang-ios-app-20260403-201144.tar.gz`；`README.md`、`Mac-首编手册.md`、`Mac-编译失败排障.md` 当前没有残留旧包名。说明“拿到 Mac 后被过期归档名带偏”这一层目前没有回归。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小改动：给 `scripts/check-ios-project.sh` 补了一层共享 `xcscheme` 静态校验；现在除了检查 scheme 文件存在，还会额外确认 `BlueprintName`、`BuildableName` 是否正确，以及 xcscheme 指向的 `BlueprintIdentifier` 目标是否仍存在于 `project.pbxproj`。这样即使以后工程 target 或 scheme 漂了，也能在拷到 Mac 前更早拦住“文件在，但 `xcodebuild` 入口已经失效”的首编坑。随后已执行 `bash scripts/check-ios-project.sh` 通过；当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小改动：把 `scripts/check-ios-project.sh` 里对 Bundle Identifier 的静态校验，从“必须等于默认 `ai.openclaw.chukouchengzhang`”改成“只要工程里存在非空合法值就通过”；这样 Mac 上一旦跑过 `scripts/configure-signing.sh` 改成自己的唯一 bundle id，后续再跑 `check-ios-project.sh` / `xcodebuild-smoke.sh` 就不会被这条过严的默认值检查误拦。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小验证：用一条 Python 对照脚本核对了 `ios-app/ChuKouChengZhang/Services/APIClient.swift` 与 `app/src/server.js`，结果 iOS 客户端当前实际调用的 10 个后端路由（`/api/bootstrap`、`/api/provider/status`、`/api/transcripts`、`/api/transcripts/create`、`/api/transcribe`、`/api/training`、`/api/train/evaluate`、`/api/ideas`、`/api/ideas/archive`、`/api/reports/daily`）在服务端都存在，HTTP 方法也对得上；说明“iOS 客户端 → 后端路由”这一层当前没有接口漂移。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小验证：实际拉起隔离临时后端实例，按 `ChuKouChengZhang/Services/RemoteModels.swift` 的解码字段要求，逐项校验了 `ProviderStatusDTO`、`BootstrapDTO`、`DailyReportDTO`、`TranscriptDTO`、`TrainingAttemptDTO`、`IdeaDTO` 对应的真实 JSON 返回；结果 `/api/provider/status`、`/api/bootstrap`、`/api/transcripts/create`、`/api/train/evaluate`、`/api/ideas/archive`、`/api/reports/daily` 当前都能返回 iOS 客户端实际解码所需字段，说明“路由存在但 JSON 结构悄悄漂了”这一层当前也没有回归。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/smoke-backend.sh` 的最轻量后端烟测从 3 条基础接口补到 4 条，新增对 `GET /api/reports/daily` 的关键字段校验，要求返回里必须有 `totalWords / transcribeCount / trainingCount / polishCount / catchphraseCount / speechInputCount / bestSentence`；随后执行 `bash scripts/handoff-backend.sh smoke` 通过，说明 Mac 接手人现在即使只跑最轻量 smoke，也能更早发现报告页接口契约有没有漂。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小验证：实际检查最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260403-234058.tar.gz` 的内部文件，确认 `app/README.md`、`app/scripts/start.sh`、`app/scripts/status.sh`、`app/scripts/stop.sh`、`app/scripts/smoke-api-flow.sh` 都已真实进包；说明拿到完整包后，后端 README、手动启动入口与完整 flow 自检入口没有在最终交付物里漏掉。当前最大阻塞点仍不是后端代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。

- 本轮仅做 1 个小验证：直接解包当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260403-234058.tar.gz`，并在包内 `ios-app/` 目录实际执行 `bash scripts/handoff-backend.sh verify` 通过；说明当前交付物本身（不是源工作区）已经能从解压后的相对目录成功定位同级 `app/`，并跑完 `npm test + smoke-api-flow` 这条自举链路，完整 handoff 包的“解压即后端总自检”路径当前没有回归。
- 本轮仅做 1 个小验证：实际拉起隔离临时后端实例，先创建 transcript、再写入 1 条 training attempt，随后请求 `GET /api/training?transcriptId=...`；结果返回 1 条记录，且 `id / transcriptId / round / text / clarityScore / structureScore / polishScore / feedback` 这组 iOS 训练页列表依赖字段都在。说明“训练页回读历史训练记录”这层后端接口当前也没有回归。
- 本轮仅做 1 个小验证：实际拉起隔离临时后端实例，先调用 `POST /api/transcripts/create` 写入 1 条 transcript，再请求 `GET /api/transcripts`；结果列表首项已包含 `id / createdAt / scene / mode / rawText / polishedText / issues` 这些 `TranscriptDTO` 必需字段，`inputSource / summaryTitle / suggestedTags / nextAction / provider` 这些 iOS 结果页与首页会用到的可选字段类型也都对得上。说明“首页/历史列表单独拉 transcripts 时 JSON 结构悄悄漂了”这一层当前也没有回归。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的完整交接包最终归档校验再补强一格；现在如果包里漏掉 `app/README.md`、`app/scripts/start.sh`、`status.sh`、`stop.sh` 或 `smoke-api-flow.sh` 这些 Mac 接手后端最常用入口，脚本会直接 fail-fast，不再只靠人工解包抽查。随后仅做了最小语法校验：`bash -n scripts/package-mac-handoff.sh` 通过。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮仅做 1 个小改动：把 `scripts/mac-env-check.sh` 的模拟器预检从“只统计任意 available 设备”补成“额外单独统计可用 iPhone Simulator 数量，并在为 0 时给出 WARN”；这样即使 Mac 上只有别的平台模拟器，也不会被误判成“iOS 首编环境已就绪”。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。
- 本轮随改随验：执行 `bash -n scripts/mac-env-check.sh` 通过；说明这次环境预检脚本改动至少在语法层面没有引入新问题。
- 本轮仅做 1 个小验证：直接解包当前最新 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260403-201144.tar.gz`，并在包内根目录实际执行 `bash scripts/check-ios-project.sh` 通过；说明当前 iOS-only 交付物本身（不是源工作区）也已经自带完整静态预检链路，拿到 Mac 后至少能先做工程完整性自检。包内校验结果当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN。
- 本轮仅做 1 个小验证：实际拉起隔离临时后端实例，先创建 1 条 transcript，再归档成 idea，随后请求 `GET /api/ideas`；结果返回数组首项已包含 `id / transcriptId / title / rawInput / normalizedText / category / tags / nextAction / status / createdAt`，且字段类型都符合 `ChuKouChengZhang/Services/RemoteModels.swift` 里的 `IdeaDTO` 预期。说明“iOS 灵感库页读取 ideas 列表时 JSON 结构悄悄漂了”这一层当前也没有回归。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境验证。

- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-ios-app.sh`，成功产出新的 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260404-091106.tar.gz`；静态预检、Xcode 工程接线校验与归档 fail-fast 检查全部通过，且 2026-04-04 这轮新增的 `scripts/mac-env-check.sh` 模拟器预检细改已经随最新 iOS-only 交付物一并打进去。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到真实 Mac 侧再签名。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“只想先编译 App / 看 UI”对应的 iOS-only 下载包文件名，从过期的 `chukouchengzhang-ios-app-20260403-201144.tar.gz` 同步到当前真实最新产物 `chukouchengzhang-ios-app-20260404-091106.tar.gz`；这样接手人按环境清单下载 iOS-only 包时，不会再被上一版归档名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接解包当前最新 iOS-only 归档 `dist/chukouchengzhang-ios-app-20260404-091106.tar.gz`，并在包内根目录实际执行 `bash scripts/check-ios-project.sh` 通过；说明最新交付出去的 iOS-only 成品本身（不是源工作区）已经能在解压后立刻完成静态工程自检，交给 Mac 接手人后至少第一步不会卡在相对路径或漏文件上。包内校验结果当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，最大阻塞点依旧是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里 `scripts/mac-env-check.sh` 的说明补齐到当前脚本行为，新增写明 `--strict` 用法，以及它现在会单独检查“是否至少有一台可用 iPhone Simulator”；如果只有别的平台模拟器，会明确给 WARN，提醒先补装 iPhone Simulator Runtime。这样接手人拿到 Mac 后，不会把“有 available 模拟器设备”误判成“iPhone 首编环境已就绪”。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。

- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成的 `HANDOFF-README.md` 里“环境预检”说明补齐到当前脚本行为，新增写明 `bash scripts/mac-env-check.sh --strict` 的用法，并明确如果提示“当前没发现可用 iPhone Simulator”，应先在 Xcode 里补装至少一套 iPhone Simulator Runtime 再继续首编；这样接手人即使只看交接包自带说明，也不会把“有别的平台模拟器”误判成“iPhone 首编环境已就绪”。随后已执行 `bash -n scripts/package-mac-handoff.sh` 通过；当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/handoff-backend.sh` 的 `--help` 文案里 `smoke` 动作说明，从“只查 `/api/provider/status`、`/api/bootstrap`、`/api/health`”补齐到当前真实行为，明确它现在还会校验 `/api/reports/daily` 以及报告页关键字段；这样接手人在 Mac 上只看脚本自带帮助时，也不会再低估这条轻量 smoke 的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里 `--backend-check smoke` 的说明，从旧的“只查 `provider/status + bootstrap + health`”补齐到当前脚本真实行为，明确它现在还会校验 `reports/daily` 与报告页关键字段；这样接手人即使只看首编主手册，也不会误判这条轻量 smoke 的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `README.md` 里 `scripts/mac-first-run.sh --backend-check smoke` 的说明，也补齐到当前脚本真实行为，明确它现在不是只做“轻量探活”，而是会检查 `provider/status`、`bootstrap`、`health`、`reports/daily`，并顺手校验报告页关键字段；这样接手人即使只先看 README，也不会低估这条轻量 smoke 的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/mac-first-run.sh` 自带 `--help` 里 `--backend-check smoke` 的说明也补齐到当前真实行为，明确它现在会检查 `provider/status`、`bootstrap`、`health`、`reports/daily`，并顺手校验报告页关键字段；随后执行 `bash scripts/mac-first-run.sh --help | grep -F -- 'reports/daily'` 命中，说明脚本自带帮助现已和 README / `Mac-首编手册.md` 对齐。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新的完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260404-134111.tar.gz`；脚本在真实打包流程里再次走通了 `npm run verify`、静态工程校验与最终 `tar.gz` 逐项命中检查，说明“从当前源工程重新打出可交付完整 handoff 包”这条主路径此刻仍是通的。当前最大阻塞点仍不是打包，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版完整 Mac 交接包 `chukouchengzhang-mac-handoff-20260403-234058.tar.gz` 的“当前最新交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处引用，统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260404-134111.tar.gz`，并已核对对应 `.sha256` 文件存在；这样接手人按环境清单拿包时，不会再被过期交接包名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接解包当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260404-134111.tar.gz`，并检查包内 `HANDOFF-README.md`；结果已命中 `bash scripts/mac-env-check.sh --strict` 与“当前没发现可用 iPhone Simulator 时先补装 Runtime”两段最新环境预检说明，说明这层交接提示不只停留在源码模板里，也已经真实进入最终交付物。当前最大阻塞点仍不是文档，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：实际执行 `bash scripts/package-mac-handoff.sh --ios-only`，成功产出 `dist/chukouchengzhang-mac-handoff-20260404-151159.tar.gz`；说明这个较少使用的 `--ios-only` 分支当前没有被后续 fail-fast / README 生成逻辑悄悄改坏，想要“只带 ios-app，但仍想拿到 HANDOFF-README 交接说明”的交付路径此刻也是通的。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，真正最大阻塞点依旧不是打包，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证。
- 本轮仅做 1 个小验证：直接解包当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260404-134111.tar.gz`，并在包内 `ios-app/` 目录实际执行 `bash scripts/check-ios-project.sh` 通过；说明完整 handoff 成品不只源码区可过静态预检，连最终交付物里的 iOS 工程自检入口也能在解压后直接跑通。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，最大阻塞点依旧不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/package-mac-handoff.sh` 对完整 Mac 交接包的后端必带清单再补强一格，新增强制校验 `app/scripts/smoke-http-provider.sh`、`app/scripts/setup-local-stt.sh`、`app/scripts/faster_whisper_warmup.py` 也必须真正进包；这样以后如果完整 handoff 包漏掉 README 里会让接手人在 Mac 上直接用到的后端 smoke / 本地 STT 辅助脚本，打包会直接 fail-fast，不再静默产出“后端能跑但自检/本地 STT 辅助脚本缺页”的半残交接包。随后已执行 `bash -n scripts/package-mac-handoff.sh`，语法通过。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接检查当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260404-134111.tar.gz` 的内部文件，确认刚纳入必带清单的 `app/scripts/smoke-http-provider.sh`、`app/scripts/setup-local-stt.sh`、`app/scripts/faster_whisper_warmup.py` 都已真实进包；说明当前完整 handoff 成品在“后端 smoke / 本地 STT 辅助脚本不能漏带”这一层已经不是停留在脚本规则上，而是现有最新产物本身也已满足。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里“完整 Mac 交接包”和较新的 `--ios-only handoff` 变体明确区分开了——当前完整闭环应下载 `chukouchengzhang-mac-handoff-20260404-134111.tar.gz`，而 `20260404-151159` 只是只含 `ios-app/ + HANDOFF-README.md` 的 `--ios-only` 变体，不能误当成带同级 `app/` 后端的完整交接包；已顺手核对这 3 个相关归档（完整 handoff / `--ios-only` handoff / iOS-only 包）及其 `.sha256` 均存在。这样拿包到 Mac 时，不会因为包类型混淆而以为“带了 handoff README 就等于带了完整后端”。当前最大阻塞点仍不是文档，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。

- 本轮仅做 1 个小改动：把 `app/scripts/smoke-api-flow.sh` 的隔离后端总自检，从原来的 `health → provider/status → bootstrap → transcripts/create → train/evaluate → ideas/archive → reports/daily`，补成会额外校验 `POST /api/transcribe`；现在 `verify` 路径也会明确检查 iOS 训练页语音转写依赖的 `text / language / duration / scene / inputSource / captureMeta / provider / segments` 这组返回字段。
- 本轮随改随验：执行 `cd /home/gem/workspace/agent/workspace/projects/出口成章/app && bash scripts/smoke-api-flow.sh` 通过，新增的 `/api/transcribe` 契约检查已真实跑通；当前隔离总自检仍能顺利完成 `health / provider/status / bootstrap / transcribe / transcripts/create / train/evaluate / ideas/archive / reports/daily` 这整条链路。
- 本轮仅做 1 个小验证：直接检查最新 `--ios-only` handoff 归档 `dist/chukouchengzhang-mac-handoff-20260404-151159.tar.gz` 的内部文件，确认它当前确实只包含 `HANDOFF-README.md + ios-app/`，不含同级 `app/` 后端；同时 `ios-app/STATUS.md`、`README.md`、`Mac-环境安装清单.md`、`scripts/mac-first-run.sh` 与 `ChuKouChengZhang.xcodeproj/project.pbxproj` 都已真实进包。说明 `Mac-环境安装清单.md` 里对这类 `--ios-only` handoff 变体的区分不是口头提醒，而是和当前最新产物实际结构一致。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/handoff-backend.sh` 自带 `--help` 里的 `smoke-flow` / `verify` 说明补齐到当前真实行为，明确它现在校验的是 `transcribe/create/train/archive/report` 全链路，而不只是旧表述里的 `create/train/archive/report`；这样 Mac 接手人即使不翻 README、只看脚本帮助，也不会低估后端总自检对 iOS 训练页语音转写链路的覆盖范围。
- 本轮仅做 1 个小改动：把 `ios-app/scripts/mac-first-run.sh --help` 里对默认 `verify` 与 `--backend-check smoke-flow` 的说明，也同步补齐成 `transcribe/create/train/archive/report` 全链路；随后执行 `bash scripts/mac-first-run.sh --help | rg 'transcribe/create/train/archive/report|默认 verify = npm test \+ smoke-flow'` 命中，说明 Mac 首编入口自带帮助已和当前后端总自检真实覆盖范围对齐。
- 本轮仅做 1 个小改动：把 `Mac-首编手册.md` 里 `--backend-check smoke-flow` 的说明，从旧的“create / train / archive / report”补齐到当前脚本真实行为 `transcribe / create / train / archive / report`；这样接手人即使只看首编主手册，也不会低估这条后端总自检对语音转写链路的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 的 `--ios-only` 产物命名改成独立前缀 `chukouchengzhang-mac-handoff-ios-only-*`，不再继续伪装成完整 `chukouchengzhang-mac-handoff-*` 包；这样接手人看文件名时，就能立刻分辨“这是只带 `ios-app/` 的 handoff 变体”，不容易把它误当成含同级 `app/` 后端的完整 Mac 交接包。本轮随改随验：`bash -n scripts/package-mac-handoff.sh` 通过，且 `bash scripts/package-mac-handoff.sh --help` 已出现“`--ios-only` 产物会额外带上 `-ios-only` 文件名前缀”提示。当前最大阻塞点仍不是打包命名，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里 `--ios-only` handoff 的命名说明补齐，明确当前 `dist/chukouchengzhang-mac-handoff-20260404-151159.tar.gz` 是改名前打出的旧产物，**下次**重新执行 `bash scripts/package-mac-handoff.sh --ios-only` 才会生成带 `-ios-only` 前缀的新文件名；这样接手人不会把“历史产物仍是旧名”误解成“脚本改名没有生效”。当前最大阻塞点仍不是文档，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `README.md` 里 `scripts/mac-first-run.sh --backend-check smoke-flow` 的说明，从旧的“完整 create/train/archive/report 烟测”补齐到当前脚本真实行为“完整 transcribe/create/train/archive/report 烟测”；这样接手人即使只看 README，也不会低估 verify / smoke-flow 对语音转写链路的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：实际执行 `bash scripts/package-mac-handoff.sh --ios-only`，成功产出新的 `--ios-only` handoff 归档 `dist/chukouchengzhang-mac-handoff-ios-only-20260404-221102.tar.gz` 与对应 `.sha256`；说明 20:10 那次对脚本的命名改动现在已经在真实产物上闭环，`--ios-only` 分支不再只是“下次会改名”，而是真的开始产出带 `-ios-only` 前缀的新包了。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN；最大阻塞点依旧不是打包命名，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里 `--ios-only` handoff 的下载指引，从仍指向改名前旧命名产物 `chukouchengzhang-mac-handoff-20260404-151159.tar.gz`，同步到当前真实最新产物 `chukouchengzhang-mac-handoff-ios-only-20260404-221102.tar.gz`，并补上对应 `.sha256`；这样接手人按环境清单找“只带 ios-app + HANDOFF-README”的 handoff 变体时，不会再被旧文件名带偏。当前最大阻塞点仍不是文档，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。

- 本轮仅做 1 个小改动：把 `scripts/package-mac-handoff.sh` 生成的 `HANDOFF-README.md` 里“后端 verify = npm test + smoke-flow”的说明补齐成“覆盖 `transcribe/create/train/archive/report` 全链路”；这样接手人即使只看交接包自带说明，也不会低估 verify 对语音转写链路的覆盖范围。
- 本轮随改随验：执行 `bash -n scripts/package-mac-handoff.sh` 通过；说明这次交接说明文案调整至少在脚本语法层面没有引入新问题。
- 本轮仅做 1 个小验证：对照检查了 `scripts/package-mac-handoff.sh` 与当时最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260404-134111.tar.gz` 内的 `HANDOFF-README.md`；结果源码模板里已经写明 verify 覆盖 `transcribe/create/train/archive/report` 全链路，但归档内说明仍停留在旧表述“`npm test + smoke-flow`”。说明当时最新完整交接包还没把这条 README 文案更新真正带进产物里；下一轮应优先重打一次完整 Mac handoff 包，把最新交接说明真正打进归档。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新的完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz`；随后直接解包检查包内 `HANDOFF-README.md`，已命中“verify 覆盖 `transcribe/create/train/archive/report` 全链路”这条最新说明。说明上轮卡住的“README 模板已更新，但最终归档还没带上”这个交付断点，现已在真实产物上闭环。当前最大阻塞点仍不是打包说明，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里仍指向旧版完整 Mac 交接包 `chukouchengzhang-mac-handoff-20260404-134111.tar.gz` 的“当前最新完整 Mac 交接包 / 现在最建议下载哪个包 / 同时下载到本地”三处引用，统一更新到当前真实最新产物 `chukouchengzhang-mac-handoff-20260405-001100.tar.gz`，并已核对对应 `.sha256` 文件存在；这样接手人按环境清单拿包时，不会再被过期交接包名带偏。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接解包当前最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz`，并在包内 `ios-app/` 目录实际执行 `bash scripts/check-ios-project.sh` 通过；说明最终完整 handoff 成品本身（不是源工作区）当前仍能在解压后直接跑通 iOS 工程静态自检入口，没有因为最近补 README / 包名 / `--ios-only` 分支说明而把工程自检链路悄悄带回去。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，最大阻塞点依旧不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：直接解包当前最新 `--ios-only` handoff 归档 `dist/chukouchengzhang-mac-handoff-ios-only-20260404-221102.tar.gz`，并在包内 `ios-app/` 目录实际执行 `bash scripts/check-ios-project.sh` 通过；说明这条“只带 `ios-app/ + HANDOFF-README.md` 去 Mac 先首编 / Mock”的 handoff 变体，本身也已具备解压后立刻做静态工程自检的能力。当前仍只有 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，最大阻塞点依旧不是交接包结构，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小改动：把 `Mac-环境安装清单.md` 里 `scripts/mac-first-run.sh` 的后端 verify 说明补齐到当前真实行为，明确它不是一句模糊的“自动做后端 verify”，而是 `npm test + smoke-flow`，并覆盖 `transcribe / create / train / archive / report` 全链路；这样接手人即使只先看环境安装清单，也不会低估这一步对语音转写与训练闭环的覆盖范围。当前最大阻塞点仍不是代码，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。

## 当前产物
- 工程目录：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/`
- Xcode 工程：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/ChuKouChengZhang.xcodeproj`
- 打包目录：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/`
- 最新 iOS-only 归档：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/chukouchengzhang-ios-app-20260404-091106.tar.gz`（本轮重新打包通过；静态预检、Xcode 工程接线校验与归档 fail-fast 检查全部通过，且 2026-04-04 这轮新增的 `scripts/mac-env-check.sh` 模拟器预检细改已经随最新 iOS-only 交付物一并打进去；当前仅剩 `DEVELOPMENT_TEAM` 为空这一条预期内 WARN，需到 Mac 侧再签名）
- iOS-only 校验文件：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/chukouchengzhang-ios-app-20260404-091106.tar.gz.sha256`
- 最新 Mac 交接包：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz`（本轮新增同步确认：这份最新完整包已经把增强版 `HANDOFF-README.md` 真实带进最终产物；其中 `verify` 说明已明确写到：除 `transcribe/create/train/archive/report` 写链路外，还会回读 `transcripts/training/ideas` 列表与 `bootstrap/reports-daily` 契约，并顺手校验 iOS 依赖字段。也就是说，之前“完整交接包 README 说明层落后于源码”的断点现在已经在最新 tar.gz / zip 产物上闭环）
- Mac 交接包校验文件：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/chukouchengzhang-mac-handoff-20260420-100846.tar.gz.sha256`
- 最新 `--ios-only` handoff 归档：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/chukouchengzhang-mac-handoff-ios-only-20260404-221102.tar.gz`（本轮实测 `bash scripts/package-mac-handoff.sh --ios-only` 通过；说明 `--ios-only` 分支现在已真正开始产出带 `-ios-only` 前缀的新命名产物。它不是完整 `ios-app + app` 交接包，只包含 `ios-app/ + HANDOFF-README.md`）
- `--ios-only` handoff 校验文件：`/home/gem/workspace/agent/workspace/projects/出口成章/ios-app/dist/chukouchengzhang-mac-handoff-ios-only-20260404-221102.tar.gz.sha256`

## 最大剩余风险
1. 真实 Mac/Xcode 首编是否一次通过（现已补命令行烟测脚本，但还没在真实 Mac 执行）
2. 真机签名（Personal Team / Bundle Identifier）是否在不同 Apple ID 下都顺利（现已补一键写入脚本）
3. 真机访问本地后端时的局域网/防火墙问题（现已补 `scripts/resolve-local-ip.sh` 帮助快速填对地址，但仍需在真实 Mac/iPhone 上验证）
4. 本地 STT / 远端 provider 在真实 Mac 环境的依赖与权限是否都一次就绪（当前后端测试通过，但还没在目标 Mac 上跑首轮）
5. Mac 侧是否存在更隐蔽的 Xcode 工程 / 签名 / 模拟器兼容问题（现已把后端与交接脚本可搬运性补齐，但真实 `xcodebuild` 结果仍待 Mac 验证）

## 距离“Xcode → 可用 iOS App”还有多远
- 代码层：主流程已基本闭环，不是当前主要阻塞
- 工程层：Xcode 工程、Info.plist、资源、脚本链路和 Mac 交接包现已基本自洽；最新 `tar.gz` 交接主路径已再次实测通过，zip 分支则仍待在有 `zip` 的 Mac 上补验
- Mac 环境层：需要真实 Mac 完成 Xcode / Node / Python 环境安装与预检
- 首编验证层：还缺一次真实 `xcodebuild` / Xcode 模拟器编译结果
- 真机可用层：还缺一次真实签名安装、麦克风权限验证、局域网后端联调验证
- 当前一句判断：离“拿到包就能在 Mac 上开始接手”已经很近；离“从 Xcode 到真正可用 iOS App”还差一次真实 Mac 首编 + 签名 + 真机网络联调

## 下一步
1. 直接在真实 Mac 上用最新完整交接包 `chukouchengzhang-mac-handoff-20260420-100846.tar.gz` 接手
2. 先跑 `bash scripts/mac-env-check.sh`，把 Xcode / xcrun / python3 / Node 依赖一次查清
3. 再跑 `bash scripts/mac-first-run.sh --team 你的TEAMID --bundle-id 你的.bundle.id`，把环境预检、静态校验、若同级 `app/` 存在则自动跑后端 verify（`npm test + smoke-flow`）、签名写入、xcodebuild 烟测一次串完
4. 若自动链路里某一步失败，再退回 `scripts/handoff-backend.sh`、`scripts/configure-signing.sh`、`scripts/xcodebuild-smoke.sh` 逐段定位，并优先使用 `build/mac-env-check.log`、`build/xcodebuild-smoke.log`、`build/xcodebuild-smoke.xcresult` 排障
5. 真机前用 `scripts/resolve-local-ip.sh` 算局域网地址
6. 根据第一轮 `xcodebuild` / 真机日志继续补首编坑

## 说明
如果没有定时器、心跳或你的新消息，我不会持续后台执行。因此现在已配套开启定时回报机制，避免“口头说继续，但实际上没继续”。
- 本轮仅做 1 个小验证：直接对照当前源码 `scripts/mac-first-run.sh --help` 与最新完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260405-001100.tar.gz` 内 `ios-app/scripts/mac-first-run.sh --help` 的实际输出，确认包内**还没有**带上“默认 verify 不只覆盖 `transcribe/create/train/archive/report` 写链路，还会回读 `transcripts/training/ideas` 列表与 `bootstrap/reports-daily` 契约”这层最新说明。最小证据很直接：源码 `--help` 已命中 `transcripts/training/ideas`、`bootstrap/reports-daily` 与增强版 `--backend-check verify` 文案；包内 `--help` 仍只停留在“`verify = npm test + smoke-flow` / `先跑 npm test，再跑 smoke-flow`”的旧表述。结论：当前完整交付物在这条 `mac-first-run.sh` 帮助层上仍落后于源码，按交付口径可视为完整产物 README/脚本帮助层尚未完全闭环；**下一轮应优先重打完整 Mac handoff 包**，把最新帮助文案真正带进产物。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮仅做 1 个小验证：重新执行 `bash scripts/package-mac-handoff.sh`，成功产出新的完整 Mac 交接包 `dist/chukouchengzhang-mac-handoff-20260420-034637.tar.gz`（以及同名 `.zip` 与对应 `.sha256`）；打包流程再次真实走通了 `npm run verify`、静态工程校验与归档命中检查。随后只对这一个断点补证据：直接检查新包内 `ios-app/scripts/mac-first-run.sh`，已命中 `transcripts/training/ideas`、`bootstrap/reports-daily` 与增强版 `--backend-check verify` 文案，说明上轮确认的“完整 handoff 包里脚本帮助落后于源码”现在已经在最新交付物上闭环。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
- 本轮严格只做了 1 个小改动：把 `Mac-首编手册.md` 里 `scripts/handoff-backend.sh verify` / `--backend-check verify` 的说明补齐到和当前 `scripts/mac-first-run.sh`、`scripts/handoff-backend.sh` 真实帮助一致，明确这条总自检不只覆盖 `transcribe / create / train / archive / report` 写链路，还会回读 `transcripts / training / ideas` 列表与 `bootstrap / reports-daily` 契约，并顺手校验 iOS 依赖字段。随后只做了最小验证：`rg -n 'transcripts / training / ideas|bootstrap / reports-daily' Mac-首编手册.md` 命中新增文案，且 `bash scripts/mac-first-run.sh --help | grep -E 'transcripts/training/ideas|bootstrap/reports-daily|--backend-check verify'` 也命中。当前最大阻塞点仍不是代码或后端接口，而是缺一次真实 Mac/Xcode 首编 + 签名 + 真机网络联调验证，其中最先卡住的仍是 Mac 环境 / 工程验证。
